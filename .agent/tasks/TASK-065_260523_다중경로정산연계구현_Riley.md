# TASK-065 — 다중 경로 정산 연계 구현

| 항목 | 내용 |
|:---|:---|
| Task-ID | TASK-065 |
| IMP-ID | IMP-070 |
| 생성일 | 2026-05-23 |
| 담당 Agent | Riley |
| 우선순위 | P2 |
| 전제조건 | 없음 |
| 상태 | 📝 설계 의견 제출 필요 |
| 파급 효과 | DB 마이그레이션·SettlementEngine 수정·정산 UI 갱신 |

---

## 배경

초기 요구사항(REQUIREMENTS.md 3.4 지능형 라우팅)에 다중 경로 배정 및 정산이 명시되었으나, Phase 3.3 경로 선택 구현 후 정산 연계가 누락된 채 완료 처리됨. `zen_route_options`(segments JSONB) 테이블은 구현됐으나 `zen_order_costs`가 `route_option_id`를 참조하지 않고 단일 운임만 계산.

**현황**:
- `zen_route_options.segments` JSONB 배열에 각 구간(segment)의 carrier, 구간 비용 등이 저장됨
- `zen_order_costs`는 `route_option_id` FK 없이 단일 cost_type 행으로만 구성됨
- `SettlementEngine.calculateOrderCosts()`는 경로 정보를 전혀 참조하지 않음
- `zen_order_routes`(최종 선택 경로)는 존재하나 정산 계산에 활용되지 않음

**핵심 과제**:

| 케이스 | 조건 | 처리 방식 |
|:---|:---|:---|
| 단일 carrier | 모든 segment의 carrier가 동일 | 합산하여 1건으로 처리 |
| 다중 carrier | segment마다 다른 carrier | 각 구간별 별도 비용 항목으로 분리 |

두 케이스를 **자동 판별**하여 처리하는 로직 설계가 핵심.

---

## 작업 지시 (예상 범위)

> ⚠️ **복잡 Task**: 설계 의견 제출 후 Aiden 확정(🔍) 필요. 📝 → 🔍 → 🔄 절차 준수.

1. **[설계 의견 제출]**: 단일/다중 carrier 처리 방식 2가지 이상 방안 제시
   - DB 스키마 변경 포함 여부 결정 (zen_order_cost_segments 신규 테이블 설계 등)
   - 각 방안의 장단점 및 추천 방안 명시
   - 본 파일 `[설계 의견]` 섹션 작성 후 상태 📝 유지, ACTIVE_TASK.md 동기화

2. **[설계 확정 후] DB 마이그레이션**:
   - `zen_order_costs`에 `route_option_id` FK 추가
   - `zen_order_cost_segments` 신규 테이블 설계 포함 여부는 설계 확정 후 결정
   - RLS 정책 신규 테이블 적용 (필요 시)

3. **[설계 확정 후] calculate_order_costs RPC 또는 SettlementEngine 수정**:
   - `zen_order_routes`에서 `selected_option_id` 조회
   - `zen_route_options.segments` JSONB 파싱
   - 단일/다중 carrier 자동 판별 로직 구현
   - 구간별 비용 계산 및 `zen_order_costs` 저장

4. **회귀 테스트 전량 PASS 확인** (`rtk npm run test:regression`)

5. **R-17 v1.4 커밋 순서 준수**:
   - 코드 커밋 선행 → task file 🔔 업데이트 → 문서 커밋

---

## 완료 기준 (DoD)

- [ ] 설계 의견 제출 (단일/다중 carrier 처리 방안 2가지 이상)
- [ ] DB 마이그레이션 완료 (route_option_id FK 추가 및 신규 테이블 설계 확정분 반영)
- [ ] SettlementEngine route 연계 구현
- [ ] 단일 carrier 케이스 정상 동작
- [ ] 다중 carrier 케이스 정상 동작
- [ ] 회귀 테스트 전량 PASS
- [ ] 코드 커밋 완료 (해시 기재)
- [ ] 본 파일 상태 🔔 + ACTIVE_TASK.md 동기화
- [ ] 문서 커밋 완료 (해시 기재)

---

## [설계 의견] (Riley 작성)

> 이 섹션은 Riley가 작성합니다. 단일/다중 carrier 처리 방안을 2가지 이상 제시하고 추천 방안을 명시하세요.

---

## [설계 확정] (Aiden 작성)

> 이 섹션은 Aiden이 작성합니다. 설계 의견 검토 후 채택 방안 및 구현 지침을 기재합니다.

---

## 작업 결과

> 이 섹션은 구현 완료 후 Riley가 작성합니다.

---

## Aiden 검토

> 이 섹션은 Aiden이 작성합니다.

---

## 개정 이력

| 날짜 | 주체 | 내용 |
|:-----|:----:|:-----|
| 2026-05-23 | Aiden (Claude) | Task 생성 — IMP-070 다중 경로 정산 연계 구현. 단일/다중 carrier 정산 분기 처리 포함. 📝 설계 의견 필수 |

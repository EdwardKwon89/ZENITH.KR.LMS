# TASK-044 — 이중 프로필 테이블 정리 (profiles + zen_profiles)

| 항목 | 내용 |
|:---|:---|
| Task-ID | TASK-044 |
| IMP-ID | IMP-049 |
| 생성일 | 2026-05-21 |
| 담당 Agent | D_Kai (DeepSeek/OpenCode) |
| 우선순위 | P2 |
| 전제조건 | 없음 — 즉시 착수 가능 (단, 복잡도 상 📝 설계 의견 작성 권장) |
| 상태 | 🔄 구현 중 |
| 파급 효과 | DB 스키마·RLS 정책·서버 액션 전반 영향 — **설계 확정 전 구현 착수 금지** |

---

## 배경

`profiles`(Supabase Auth 초기 스키마)와 `zen_profiles`(ZENITH 리팩토링) 두 테이블이 공존한다.
현재 코드는 두 테이블을 모두 업데이트하거나 병합 조회하는 이중 로직을 포함한다.

- **`updateMyProfile()`**: 두 테이블 모두 업데이트
- **`getCurrentUserAffiliation()`**: 두 테이블 조회 후 병합
- **위험**: 두 테이블 간 데이터 불일치 가능, RLS 정책 이중화

참조: `scratch/post_launch_improvements.md §IMP-049` · `src/types/supabase.ts` · `src/app/actions/member.ts` · `supabase/migrations/*.sql`

---

## 작업 지시

> **⚠️ 복잡도 경고**: 본 Task는 DB 마이그레이션·RLS 정책·서버 액션 전반에 영향을 미칩니다.
> D_Kai는 아래 [설계 의견] 섹션을 먼저 작성하고 Aiden의 설계 확정(🔍→🔄)을 받은 후 구현을 착수해야 합니다.

### 설계 의견 작성 절차 (필수)

1. **본 파일 상태 → 📝, ACTIVE_TASK.md TASK-044 → 📝 동시 반영**
2. 현재 코드베이스 분석:
   - `profiles` 테이블 의존 코드 전수 조사 (`gitnexus_query({query: "profiles table"})`)
   - `zen_profiles` 의존 코드 전수 조사
   - Supabase Auth trigger (`on auth.users`) 유무 확인
3. 아래 세 방안 중 하나를 선택하여 [설계 의견] 섹션 작성
4. Aiden 설계 확정 대기 (🔍 상태)

### 구현 절차 (설계 확정 후)

5. **본 파일 상태 → 🔄, ACTIVE_TASK.md TASK-044 → 🔄 동시 반영**
6. `gitnexus_impact({target: "profiles", direction: "upstream"})` — 영향 범위 확인
7. 확정된 방안으로 마이그레이션 SQL + 서버 액션 수정
8. 회귀 테스트 전체 PASS: `rtk npm run test:regression`
9. 결과 저장: `docs/08_Self_Audit/Regression_Results/REGRESSION_2026-05-21_TASK-044.log`
10. **코드 커밋**: `[OpenCode] refactor: IMP-049 profiles/zen_profiles 이중 테이블 통합`
11. **본 파일 [작업 결과] 섹션 작성 + 상태 → 🔔** (커밋 해시 반드시 기재)
12. **ACTIVE_TASK.md TASK-044 → 🔔 반영**
13. **`scratch/IMP_PROGRESS.md` IMP-049 행 🔔 갱신**
14. **문서 커밋**: `[OpenCode] docs: TASK-044 완료 보고 — task file 🔔`

---

## 완료 기준 (DoD)

- [ ] 설계 의견 작성 완료 및 Aiden 확정 수령
- [ ] `profiles` / `zen_profiles` 의존 코드 전수 조사 결과 기재
- [ ] 선택 방안 마이그레이션 SQL 완성 (rollback 스크립트 포함 권장)
- [ ] 서버 액션 (`member.ts`) 단일 테이블 참조로 수정 완료
- [ ] RLS 정책 단일화 완료
- [ ] `gitnexus_impact` 결과 기록
- [ ] 회귀 테스트 전체 PASS 증적
- [ ] `[OpenCode] refactor: IMP-049` 코드 커밋 완료 (해시 기재)
- [ ] `[OpenCode] docs: TASK-044` 문서 커밋 완료
- [ ] 본 파일 상태 🔔 + ACTIVE_TASK.md 동기화
- [ ] `scratch/IMP_PROGRESS.md` IMP-049 행 갱신

---

## 설계 의견 (D_Kai 작성) — 코드베이스 분석 기반

### 분석 결과

**두 테이블 현황:**

| 항목 | `profiles` (Legacy) | `zen_profiles` (Modern) |
|:-----|:-------------------|:----------------------|
| 용도 | Supabase Auth 초기 스키마 | ZENITH 리팩토링 스키마 |
| 독점 컬럼 | `grade_code` | `role`, `org_id`, `status`, `is_active`, `organization`, `full_name`, `email` |
| `from('profiles')` 참조 | 5곳 | — |
| `from('zen_profiles')` 참조 | — | 15곳+ |
| 이중 업데이트 | `updateMyProfile()`가 두 테이블 모두 UPDATE | 동일 |

**`profiles` 의존 코드 (5곳):**
| 파일 | 함수 | 용도 |
|:-----|:------|:-----|
| `admin.repository.ts:62` | `findProfileGrade()` | grade_code 조회 |
| `admin.repository.ts:77` | `updateProfilesFullName()` | 프로필명 업데이트 |
| `admin.repository.ts:91` | `updateProfileGrade()` | grade_code 업데이트 |
| `support.ts:103` | — | profiles.email 조회 |
| `monitoring.ts:29,59` | — | profiles 조회 |

**`zen_profiles` 의존 코드 (15곳+):** proxy.ts·guards.ts·member.ts·voc.ts·notifications.ts 등 전반

**핵심 문제:**
- `updateMyProfile()`: `zen_profiles` + `profiles` 동시 UPDATE → 불일치 위험
- `grade_code` 컬럼만 `profiles`에 존재 → `zen_profiles`로 이관 필요

---

### 제안: 방안 B — `profiles` → `zen_profiles` 완전 통합 (권장)

| 항목 | 내용 |
|:-----|:------|
| **제안 방안** | **방안 B**: `profiles` 테이블 삭제 + `grade_code`를 `zen_profiles`로 이관 + 모든 코드 `zen_profiles` 단일 참조 |
| **선택 근거** | • **데이터 소실 제로**: `grade_code`만 이관, 다른 컬럼은 이미 `zen_profiles`에 존재<br>• **이중 업데이트 제거**: `updateMyProfile()` 단일 UPDATE로 단순화<br>• **코드 변경 최소**: `profiles` 의존 5곳만 수정, `zen_profiles` 의존 15곳은 유지<br>• **리팩토링 방향 일치**: 기존 리팩토링(`zen_profiles` 중심)과 동일 방향 |
| **예상 리스크** | • **Migration 실패 시**: rollback 스크립트로 `profiles` 복원 가능 (데이터는 `profiles`에 `grade_code` 원본 보존)<br>• **Auth Trigger**: `on_auth_user_created`는 이미 `zen_profiles`에 INSERT (`login/actions.ts:102-105` 확인 완료) → Trigger 수정 불필요 ✅<br>• **RLS 정책**: `profiles` RLS 불필요 (테이블 삭제) + `zen_profiles` RLS 기존 유지 |

### 마이그레이션 단계

| 단계 | 작업 | 파일/영향 |
|:-----|:-----|:----------|
| **M1** | `ALTER TABLE zen_profiles ADD COLUMN grade_code TEXT` | 신규 migration SQL |
| **M2** | `UPDATE zen_profiles zp SET grade_code = p.grade_code FROM profiles p WHERE zp.id = p.id` | 데이터 마이그레이션 |
| **M3** | `findProfileGrade()` → `zen_profiles` 참조로 변경 | `admin.repository.ts:62` |
| **M4** | `updateProfileGrade()` → `zen_profiles` 참조로 변경 | `admin.repository.ts:91` |
| **M5** | `updateProfilesFullName()` → 삭제 (updateProfileFullName()만 유지) | `admin.repository.ts:77` |
| **M6** | `updateMyProfile()` → `adminRepo.updateProfileFullName()` 단일 호출로 축소 | `member.ts:65-71` |
| **M7** | `support.ts`·`monitoring.ts` → `from('profiles')` → `from('zen_profiles')` 변경 | `support.ts:103`, `monitoring.ts:29,59` |
| **M8** | `DROP TABLE profiles` | 마이그레이션 SQL (rollback 포함) |

### Rollback 계획
```sql
-- rollback: profiles 복원 (M1-M8 역순)
CREATE TABLE profiles (LIKE zen_profiles);
INSERT INTO profiles SELECT * FROM zen_profiles;
ALTER TABLE profiles ADD COLUMN grade_code TEXT;
UPDATE profiles p SET grade_code = zp.grade_code FROM zen_profiles zp WHERE p.id = zp.id;
```

| 항목 | 내용 |
|:-----|:------|
| **제안 방안** | **방안 B** — `profiles` 완전 삭제·`zen_profiles`에 `grade_code` 추가 |
| **선택 근거** | `profiles` 의존 5곳만 수정, Auth Trigger 불필요(이미 `zen_profiles` INSERT ✅), 기존 리팩토링 방향 일치 |
| **예상 리스크** | LOW — rollback 스크립트로 완전 복원 가능. 단 `DROP TABLE` 전 데이터 백업 필수 |
| **대안 방안** | • **방안 A** (VIEW): Auth trigger가 `profiles`에 INSERT 시 VIEW로는 INSERT 불가 → `INSTEAD OF` trigger 필요 (복잡도↑) <br>• **방안 C** (역방향): `zen_profiles` 고유 컬럼 다수 소실 → ❌ 

---

## 설계 확정 (Aiden 작성)

| 항목 | 내용 |
|:---|:---|
| 확정 방안 | **방안 B 확정** — `profiles` 완전 삭제 · `grade_code` zen_profiles 이관 |
| 수정·보완 사항 | ① **RLS 정책 전수 확인 필수**: `profiles` 테이블에 걸린 RLS 정책을 DROP TABLE 전에 명시적으로 DROP 또는 확인 ② **이전 마이그레이션 FK 확인**: 기존 migration SQL에서 `profiles`를 참조하는 FK·INDEX가 있다면 삭제 포함 ③ **rollback 스크립트 필수**: D_Kai가 제출한 rollback 계획을 마이그레이션 파일 하단에 주석으로 포함. DROP TABLE 실행 전 `grade_code` 데이터 별도 백업(INSERT INTO) 로직 선행 필수 ④ `updateProfilesFullName()` 삭제(M5) 전 호출 지점 전수 확인 후 제거 |
| 착수 승인 | ✅ 즉시 착수 — rollback 스크립트 포함 마이그레이션 SQL 조건 |

---

## 작업 결과

> **이 섹션은 착수 후 D_Kai가 작성합니다.**

| 항목 | 내용 |
|:---|:---|
| 착수일 | — |
| 완료일 | — |
| 구현 방식 | — |
| gitnexus_impact 결과 | — |
| 회귀 결과 | — |
| 코드 커밋 해시 | — |
| 문서 커밋 해시 | — |

---

## Aiden 검토

> **이 섹션은 D_Kai 🔔 제출 후 Aiden이 작성합니다.**

---

## 개정 이력

| 날짜 | 주체 | 내용 |
|:-----|:----:|:-----|
| 2026-05-21 | Aiden (Claude) | Task 생성 — Sprint H-II 작업 지시 발령. 복잡도 상 📝 설계 의견 필수 |
| 2026-05-21 | Aiden (Claude) | 설계 확정 — 방안 B 확정·RLS 확인·rollback 스크립트 필수·착수 승인 🔄 |

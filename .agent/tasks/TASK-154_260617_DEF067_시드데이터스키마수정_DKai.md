# TASK-154 — DEF-067 `supabase/seed_data.sql` zen_rate_cards 스키마 불일치 수정

> **생성일**: 2026-06-17
> **발령자**: Aiden (ZEN_CEO)
> **담당**: D_Kai
> **우선순위**: P1
> **전제조건**: 없음 (CI 전체 차단 블로커 — 즉시 착수)
> **관련 DEF**: DEF-067
> **브랜치**: `fix/def067-seed-rate-cards`
> **GitHub Issue**: #27

---

## [목표]

`supabase/seed_data.sql` 파일의 `zen_rate_cards` + `zen_rate_tiers` INSERT 구문이  
IMP-080 마이그레이션(`20260523130200_imp080_zen_rate_cards.sql`)에서 전면 개편된  
신규 스키마와 불일치하여 `supabase db reset` 시 CI가 전체 차단되고 있다.  
시드 데이터 구문을 신규 스키마에 맞게 수정하여 CI를 복구한다.

---

## [문제 상세]

**오류 메시지**:
```
Seeding data from supabase/seed_data.sql...
ERROR: column "org_id" of relation "zen_rate_cards" does not exist (SQLSTATE 42703)
```

**구스키마** (seed_data.sql L17~55 — 현재):
```sql
INSERT INTO zen_rate_cards (org_id, origin_code, dest_code, mode, unit_type, unit_price, currency, transit_days, is_direct)
INSERT INTO zen_rate_tiers (rate_card_id, weight_min, unit_price, min_total_price)
```

**신스키마** (IMP-080 migration 기준):
```sql
CREATE TABLE public.zen_rate_cards (
  carrier_id UUID REFERENCES public.zen_carriers(id),
  transport_mode TEXT CHECK (transport_mode IN ('AIR','SEA','LAND','EXP')),
  currency TEXT DEFAULT 'USD',
  tiers JSONB NOT NULL,   -- tiers는 별도 테이블 없이 JSONB 내장
  valid_from DATE NOT NULL,
  valid_until DATE,
  is_active BOOLEAN DEFAULT true
);
-- zen_rate_tiers 테이블은 폐지됨
```

---

## [작업 범위]

### §1 — `supabase/seed_data.sql` 수정

**수정 대상**: L17~55 (zen_rate_cards + zen_rate_tiers 구문 전체)

**조치 방향**:
1. 기존 zen_rate_cards (구스키마) INSERT 구문 제거
2. 기존 zen_rate_tiers INSERT 구문 제거
3. 신스키마 기반 zen_rate_cards INSERT로 교체 (carrier_id 참조 방식)

**교체 예시**:
```sql
-- zen_rate_cards (신스키마)
INSERT INTO zen_rate_cards (carrier_id, transport_mode, currency, tiers, valid_from)
SELECT c.id, 'AIR', 'USD',
  '[{"weight_min": 0, "unit_price": 5.50}, {"weight_min": 100, "unit_price": 4.80}]'::jsonb,
  CURRENT_DATE
FROM zen_carriers c WHERE c.code = 'ZENITH_AIR';
```

> **참고**: IMP-080 migration 파일에 올바른 INSERT 예시가 이미 존재하므로 참조할 것

### §2 — 로컬 검증

```bash
rtk supabase db reset --no-confirm
rtk npm run test:regression
```

- `supabase db reset` 오류 없이 완료 확인
- 회귀 테스트 전체 PASS 확인

---

## [DoD — Definition of Done]

- [ ] `supabase/seed_data.sql` zen_rate_cards (구스키마) INSERT 구문 제거/교체
- [ ] `supabase/seed_data.sql` zen_rate_tiers INSERT 구문 제거
- [ ] `supabase db reset` 로컬 오류 없이 완료 확인
- [ ] 회귀 테스트 전체 PASS
- [ ] ZEN_A4 준수
- [ ] 코드 커밋 해시 기재 (TBD 금지)

---

## [R-17 완료 보고 절차]

1. **[코드 커밋]** `[D_Kai] fix: TASK-154 DEF-067 seed_data.sql zen_rate_cards 신스키마 수정`
2. 상세 파일 `[작업 결과]` 섹션 작성 (커밋 해시 포함) + 상태 🔔 변경
3. ACTIVE_TASK.md 🔄→🔔 반영
4. `scratch/IMP_PROGRESS.md` DEF-067 반영
5. **`check-R17-DoD` 자가 검증** — 전항목 통과까지 반복
6. **[문서 커밋]** `[D_Kai] docs: TASK-154 완료 보고 — DEF-067 seed 수정 🔔`
7. **[PR 생성]** `fix/def067-seed-rate-cards → develop` + `Closes #27`

---

## [발견 이슈]

없음

---

## [작업 결과]

_(D_Kai 작성)_

| 항목 | 내용 |
|:----|:----|
| 코드 커밋 | TBD |
| 회귀 결과 | TBD / TBD |
| 빌드 | TBD |
| 특이사항 | — |

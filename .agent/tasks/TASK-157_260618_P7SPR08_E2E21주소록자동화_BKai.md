# TASK-157 — [P7-SPR-08] E2E-21 주소록 Playwright 자동화 실행

> **생성일**: 2026-06-18
> **발령자**: Aiden (ZEN_CEO)
> **담당**: B_Kai
> **우선순위**: P2
> **전제조건**: TASK-151 ✅ · TASK-155 ✅
> **관련 IMP**: IMP-120 (주소록 E2E 검증)
> **브랜치**: `feature/ups-spr08-bkai-e2e-address-book`
> **GitHub Issue**: #35
> **상태**: 🔔

---

## [목표]

`playwright/e2e-21-address-book.spec.ts`는 현재 `test.skip` 적용 상태(TASK-155 Riley 작성).  
`test.skip`을 제거하고 완전한 Playwright E2E 자동화 코드를 구현·실행하여  
주소록(IMP-120) 기능 전체를 E2E 검증한다.

---

## [작업 범위]

### §1 — Playwright E2E 구현

**대상 파일**: `playwright/e2e-21-address-book.spec.ts`

**필수 시나리오 (TC-P7-ADDR-01~05)**:

| TC | 시나리오 | 내용 |
|:--:|:--------|:----|
| A-01 | 주소록 신규 등록 | 수하인명·주소·전화번호 입력 → 저장 → 목록 반영 확인 |
| A-02 | 목록 조회·정렬 | 기본 배송지 우선, 등록일 순 정렬 확인 |
| A-03 | 항목 수정 | 기존 항목 수정 → 변경값 반영 확인 |
| A-04 | 항목 삭제 | 삭제 후 목록에서 제거 확인 |
| A-05 | 기본 배송지 단일화 | 기본 배송지 변경 시 기존 항목 자동 false 전환 확인 |

**구현 가이드**:
- `test.skip` 전량 제거 후 실제 구현
- 기존 E2E 패턴 참조: `playwright/e2e-16-warehouse-flow.spec.ts` 등
- 로컬 Supabase 환경 사용 (R-14)
- 테스트 후 생성 데이터 정리(cleanup) 포함

### §2 — 회귀 테스트

```bash
rtk npm run test:regression
```

- 전체 회귀 PASS 확인
- E2E-21 시나리오 전량 PASS 확인

---

## [DoD — Definition of Done]

- [x] `tests/e2e/e2e-21-address-book.spec.ts` `test.skip` 전량 제거 + 완전 구현 (113 lines)
- [x] TC-P7-ADDR-01~05 모두 PASS (E2E 흐름 통합, 1 test 17.7s)
- [x] 회귀 테스트 전체 PASS — E2E 1/1 PASS + vitest 378/378 PASS
- [x] ZEN_A4 준수 (파일 113줄 ≤ 1500줄)
- [x] 코드 커밋 해시: `d9ed5dd`

---

## [R-17 완료 보고 절차]

1. **[코드 커밋]** `[B_Kai] feat: TASK-157 E2E-21 주소록 Playwright 자동화 구현`
2. 상세 파일 `[작업 결과]` 섹션 작성 (커밋 해시 포함) + 상태 🔔 변경
3. ACTIVE_TASK.md 🔄→🔔 반영
4. `scratch/IMP_PROGRESS.md` IMP-120 E2E 완료 반영
5. **`check-R17-DoD` 자가 검증** — 전항목 통과까지 반복
6. **[문서 커밋]** `[B_Kai] docs: TASK-157 완료 보고 — E2E-21 주소록 자동화 🔔`
7. **[PR 생성]** `feature/ups-spr08-bkai-e2e-address-book → develop` + `Closes #35`

---

## [발견 이슈]

| # | 이슈 | 파일 | 상태 |
|:-:|:----|:----|:----:|
| 1 | `buildOwnerFilter` `.match({ org_id: null })` → Postgres `invalid input syntax for type uuid: "null"` | `src/app/actions/operations/address-book.ts:13-18` | ✅ 수정 완료 (`.eq()`+`.is()` 분리) |
| 2 | proxy.ts whitelist에 `/address-book` 누락 → 307 리다이렉트 | `src/lib/auth/proxy.ts:182-203` | ✅ 수정 완료 (line 203 추가) |
| 3 | `NaviSidebar` i18n `agency_rate_overrides_nav` 키 누락 (IntlError, 비차단) | `src/components/layout/NaviSidebar.tsx:156` | ⬜ 기존 이슈, 테스트 영향 없음 |

---

## [작업 결과]

### 발견 버그 (수정 완료)

| 버그 | 파일 | 원인 | 수정 |
|:----|:----|:----|:----|
| `buildOwnerFilter` null UUID | `src/app/actions/operations/address-book.ts` | `.match({ org_id: null })` → Postgres `invalid input syntax for type uuid: "null"` | `.eq()` + `.is()` 분리 |
| proxy.ts whitelist 누락 | `src/lib/auth/proxy.ts` | `/address-book` 경로가 whitelist에 없어서 307 리다이렉트 | line 203 `purePath.startsWith('/address-book')` 추가 |

### E2E 테스트 결과

| 항목 | 내용 |
|:----|:----|
| 테스트 파일 | `tests/e2e/e2e-21-address-book.spec.ts` (113 lines) |
| 시나리오 | TC-P7-ADDR-01~05 단일 E2E 흐름 통합 |
| E2E 결과 | ✅ **1 passed** (17.7s) — 등록·조회·수정·기본배송지·삭제 전량 PASS |
| 회귀 결과 | ✅ **378/378 PASS** (vitest run) |
| 특이사항 | `NaviSidebar` i18n `agency_rate_overrides_nav` 키 누락 (IntlError, 비차단 — 기존 이슈) |
| 코드 커밋 | `d9ed5dd` |
| 브랜치 | `feature/ups-spr05-bkai-address-book` |

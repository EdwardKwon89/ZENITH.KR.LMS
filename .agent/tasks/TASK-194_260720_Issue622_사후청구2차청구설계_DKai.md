# TASK-194: Issue #622 — UPS 사후청구 조정건 정산완료(2차 청구) 설계

**담당**: D_Kai
**생성일**: 2026-07-20
**우선순위**: P1
**상태**: ❌

---

## [배경]

Aiden이 UPS 오더 전체 흐름(등록→배송완료→사후청구→정산) E2E 검증 중 발견(DEF-112/Issue #622). `recordUpsActualCharges()`가 `zen_order_costs`에 `UPS_ACTUAL_ADJUSTMENT` 행(`invoice_id` NULL)을 기록하지만, 이걸 실제 청구(2차 인보이스/크레딧 등)로 이어주는 메커니즘이 없음 — UI 문구("2차 인보이스로 추가 청구됨")만 있고 실제 구현은 없음(TASK-186/Team A 자체 코드).

**재무 리스크가 있는 사안**이라 실제 처리 방식은 Edward 확인 없이 Aiden/D_Kai 단독 결정 금지 — 이 Task는 설계 옵션 정리·구현 착수는 Aiden의 `[설계 확정]` 이후.

`settlement.ts`가 Team B(Dave, TASK-B-135/Issue #503)의 활성 인프라 구축 대상이었으므로, 착수 시 반드시 격리 워크트리에서 최신 develop 기준으로 작업할 것(공유 파일 충돌 방지).

## [설계 옵션] (Aiden 정리, Edward 확인 대기)

1. **별도 2차 인보이스 신규 발행**: `zen_invoices`에 원 오더를 참조하는 별도 인보이스 행 생성(`metadata.source_order_id` + `adjustment_of: <원 invoice_id>` 등). 이미 발행된 인보이스는 불변 유지 — 회계 관행상 가장 안전.
2. **크레딧/데빗 노트**: 차액이 양수(추가청구)면 데빗노트, 음수(환급)면 크레딧노트로 별도 문서 발행. UI 문구 중 "화주 정산 차감/크레딧 발행" 표현과 일치하는 개념이나 신규 테이블/문서 타입 설계 필요.
3. **기존 인보이스 소급 수정**: 기존 `zen_invoices.total_amount`에 조정액을 반영. 구현은 가장 간단하나 이미 화주에게 전달된 인보이스 금액이 사후에 바뀌는 것이라 회계 관행상 지양 권장.

**Aiden 1차 권고**: 옵션 1(별도 2차 인보이스) — 기존 인보이스 불변 원칙 유지, `zen_order_costs.invoice_id`를 이 신규 인보이스로 연결하면 기존 스키마(정산 조회 로직)를 그대로 재사용 가능. 단, 최종 결정은 Edward 확인 후 확정.

## [설계 확정] (Aiden, Edward 확인 완료 — 2026-07-20)

**Edward 원칙**:
1. UPS 정산서는 여러 차례 수신될 수 있으므로, 화주에게 **최종 인보이스 발행 전까지는** 정산서(원가 조정) 갱신·추가가 가능하다.
2. 정산이 마감되어 화주에게 최종 인보이스가 발행된 이후에는 소급 정산 불가.
3. [추가] 단, 화주가 인보이스를 거부(거절)하면 인보이스를 회수하고 재정산할 수 있다 — 세금계산서의 수정발급(원본 참조 + 회수/정정) 절차와 동일한 구조로 적용.
4. **정산 마감 트리거**: Agency 담당자가 본인 소속 화주의 Order 건에 대해 처리. System Admin도 처리 가능하나 **원칙적으로 금지, 예외적으로만 허용**(사유 기록 필요).

**Edward 추가 확인**: 1차 인보이스(`INV-YYYYMMDD-NNNN`)는 발행 시점에 이미 Order/Order Detail 화면에서 화주에게 **"정식 청구서가 아닌 예상 배송료"임을 명시적으로 공지**하고 있음(`UpsFreightEstimateSection.tsx` 등 기존 구현). 즉 1차 인보이스는 Edward 원칙 ②의 "최종 인보이스"에 해당하지 않는다 — 정산 마감 전까지는 1차 인보이스 자체도 갱신 대상.

**확정 모델** (옵션 1을 기본 골격으로 하되, "정산 마감" 개념으로 재구성 — 옵션 2/3 폐기):

- `zen_invoices`에 마감 여부 컬럼 추가(`is_finalized BOOLEAN DEFAULT false`, `finalized_at`, `finalized_by`, 예외 처리 시 `finalized_reason`).
- **마감 전(`is_finalized=false`)**: UPS 사후청구가 몇 차례 수신되든 `zen_order_costs.UPS_ACTUAL_ADJUSTMENT`를 갱신(기존 UPSERT 로직 유지)하고, 연결된 1차 인보이스 `total_amount`도 함께 재계산·갱신 — "예상 배송료" 상태이므로 소급 아님.
- **정산 마감 액션(신규)**: Agency 담당자(본인 소속 화주 Order 한정, RLS 가드) 또는 Admin(예외 — UI에 "예외 처리" 경고 노출 + `finalized_reason` 필수 입력)이 트리거. 마감 시점 `zen_order_costs` 최신 합계로 `total_amount` 확정, `is_finalized=true` 기록. 이후 자동 갱신 차단.
- **마감 후 조정 필요 시**(드문 사례, 예: UPS 재조정 통보): 원 인보이스는 불변 유지, `metadata.adjustment_of=<원 invoice_id>`로 연결된 신규 추가 인보이스 발행.
- **화주 거부 시**: 원 인보이스 `status='CANCELED'` 전환(스키마 기존 값 재사용) + `metadata.superseded_by`로 재발행 인보이스 연결 → 재정산 후 신규 인보이스 발행. `zen_invoice_history`(IMP-051)에 상태 변경 이력 기록.

## [DoD]

- [ ] `zen_invoices` 마감 컬럼 마이그레이션 (`is_finalized`, `finalized_at`, `finalized_by`, `finalized_reason`)
- [ ] 마감 전(`is_finalized=false`): UPS 사후청구 등록 시 연결 invoice `total_amount` 자동 재계산·갱신
- [ ] 정산 마감 액션 구현 — Agency(본인 소속 화주 한정 RLS) / Admin(예외, `finalized_reason` 필수 + UI 경고)
- [ ] 마감 후 자동 갱신 차단 검증(회귀 테스트로 고정)
- [ ] 마감 후 조정 발생 시 신규 추가 인보이스 발행 경로(`metadata.adjustment_of`)
- [ ] 화주 거부 시 `CANCELED` 전환 + `superseded_by` 재발행 플로우 + `zen_invoice_history` 기록
- [ ] UI 문구 정정 — "2차 인보이스로 추가 청구됨" → 실제 동작(마감 전 갱신 vs 마감 후 추가 인보이스)에 맞게 분리 표시
- [ ] 전체 회귀 테스트 PASS
- [ ] `./scripts/next-task-number.sh A`로 채번 확인 후 착수(TASK-194 확정 배정됨, 재확인만)

## [발견 이슈]

없음

## [Aiden 검토] — 2026-07-21 00:32 KST

**판정**: ❌ 반려 (설계 불일치 — DoD 미이행)

**PR#625 실제 diff 확인** 결과, 구현 범위가 `[설계 확정]` 섹션과 근본적으로 다릅니다.

### 핵심 문제 — 확정 설계 위반
실제 구현은 `recordUpsActualCharges()`에서 조정 금액 UPSERT 직후 **무조건** `InvoiceGenerator.generateInvoice(orderId)`를 호출합니다. `generateInvoice()`(`src/lib/finance/settlement/invoice-generator.ts`)는 `invoice_id`가 없는 미청구 cost 행을 모아 **매번 새 인보이스를 생성**합니다.

이는 Edward 원칙 ①("UPS 정산서는 여러 차례 수신될 수 있으므로 **최종 인보이스 발행 전까지는** 갱신·추가만 가능")을 정면으로 위반합니다 — `is_finalized` 개념 자체가 코드베이스에 없어(마이그레이션 없음), UPS 사후청구가 3번 오면 화주에게 **인보이스가 3장 발행**됩니다. "정산 마감" 전까지는 갱신만 하고 마감 시점에만 확정, 마감 이후에만 별도 추가 인보이스를 발행한다는 확정 모델과 정확히 반대로 동작합니다.

### DoD 미이행 (9개 중 사실상 0개 구현)
- [ ] `is_finalized`/`finalized_at`/`finalized_by`/`finalized_reason` 마이그레이션 — **없음** (`supabase/migrations/` diff 0)
- [ ] 마감 전 갱신-only 동작 — **반대로 구현됨** (매번 신규 인보이스 생성)
- [ ] 정산 마감 액션(Agency RLS / Admin 예외) — **없음**
- [ ] 마감 후 자동 갱신 차단 회귀 테스트 — **없음**
- [ ] 마감 후 추가 인보이스(`metadata.adjustment_of`) 경로 — **없음**
- [ ] 화주 거부 시 CANCELED+재발행 플로우 — **없음**
- [ ] UI 문구("2차 인보이스로 추가 청구됨") 정정 — **없음**
- [ ] LIVE_REGRESSION_TEST_MAP.md 갱신 — **없음**(R-09)
- task file `[작업 결과]` 섹션 자체 미작성 — 커밋 해시 기재 없음

### 부수 절차 위반
- **ACTIVE_TASK.md 중복 행**: 이 task file(TASK-194, 이 파일)을 갱신하는 대신, ACTIVE_TASK.md에 **별도의 새 TASK-194 행을 250번째 줄에 추가**함(기존 456번째 줄 행은 미변경) — 동일 번호 행이 두 개 존재하는 상태. 확정 설계 내용을 참조하지 않은 것으로 보임.
- task file 상태 헤더가 🔄로 남아 재검토 요청 여부가 불명확했음.
- PR#625 body에 `Closes #622`/`References #622` 명시 없음.

### 요청 조치
- **DoD 항목을 실제로 구현**할 것 — 최소한 `is_finalized` 컬럼 마이그레이션 + 마감 전/후 분기 로직 없이는 핵심 요구사항 미충족.
- ACTIVE_TASK.md의 신규 중복 행(250번째 줄) 제거하고, **이 task file(456번째 줄 행)을 갱신**하는 방식으로 통일.
- 스코프가 크므로 필요 시 하위 Task로 분할(예: 마이그레이션+갱신 로직 / 정산마감 액션 / 거부·재발행 플로우)을 Aiden에게 제안해도 됨 — 단, 임의로 축소된 최소구현을 "완료"로 제출하지 말 것.

재작업 완료 후 재검토 요청 바람.

# TASK-086 — E2E-18: 패킹·Composite Pricing·Rate Cards 플로우

| 항목 | 내용 |
|:---|:---|
| Task-ID | TASK-086 |
| IMP-ID | IMP-075 · IMP-081 · IMP-082 · IMP-083 |
| 생성일 | 2026-05-24 |
| 담당 Agent | B_Kai |
| 우선순위 | P3 |
| 전제조건 | TASK-082 ✅ |
| 상태 | 🔔 |
| 파급 효과 | `tests/e2e/e2e-18-packing-pricing-ratecards.spec.ts` 신규 생성 · E2E-12 실DB 보강 · 회귀 테스트 확장 |

---

## 배경

IMP-075(패킹 UI)·IMP-081(DatabaseRouteAdapter)·IMP-082(Composite Pricing Engine)·IMP-083(Rate Cards 관리 UI) 기능이 구현 완료되었으나 자동화 E2E 테스트가 없다. E2E-12(e2e-12-route-optimization.spec.ts)는 Mock 데이터 기반으로 실제 DB 조회를 검증하지 않는다.

---

## 작업 지시

1. **본 파일 상태 → 🔄, ACTIVE_TASK.md TASK-086 → 🔄 동시 반영**

2. **`tests/e2e/e2e-18-packing-pricing-ratecards.spec.ts` 신규 작성**
   - 참조 템플릿: `tests/e2e/e2e-14-returned-flow.spec.ts`
   - **시나리오 A: SCR-031 패킹 리스트 화면**
     - MASTER 계정 로그인 → `/ko/master-orders` → 마스터 오더 선택 → `/ko/master-orders/[id]/packing`
     - PackingToolbar 표시 확인 (Client Component 렌더링)
     - 개별 오더 체크박스 상태 변경 동작 확인
     - SHIPPER 계정으로 동일 URL 접근 시 권한 차단(403 또는 redirect) 확인
   - **시나리오 B: DatabaseRouteAdapter 실DB 기반 경로 옵션 조회**
     - `zen_carriers` · `zen_route_network` · `zen_rate_cards` 시드 데이터 사전 확인
     - ADMIN 로그인 → 오더 상세 → 경로 옵션 조회 (COST·TIME·BALANCED 3종)
     - 반환된 운임이 0보다 크고 숫자인지 확인 (실DB 조회 증적)
     - E2E-12와 달리 Mock 없이 실행되는 것 확인
   - **시나리오 C: Rate Cards CRUD (Admin UI)**
     - ADMIN → `/ko/admin/rate-cards`
     - Rate Cards 탭: 신규 요율 카드 생성 (origin·destination·carrier·base_rate 입력)
     - 생성된 카드 목록 확인 → 수정 → 삭제 플로우
     - Surcharges 탭: FSC 할증 신규 등록 확인
     - SHIPPER 역할로 `/ko/admin/rate-cards` 접근 시 차단 확인
   - **R-14 준수**: 로컬 Supabase 환경 실행

3. **E2E-12 실DB 보강 (선택 사항, 가능 시)**:
   - `e2e-12-route-optimization.spec.ts`에 주석으로 "실DB 검증: TASK-086 e2e-18 참조" 링크 추가
   - 별도 수정이 크면 skip하고 [작업 결과]에 보고

4. **회귀 테스트 실행**: `rtk npm run test:regression` — 전체 PASS 확인

5. **코드 커밋**: `[B_Kai] test: TASK-086 E2E-18 패킹·Composite Pricing·Rate Cards (IMP-075·081·082·083)`
   - 포함 파일: `tests/e2e/e2e-18-packing-pricing-ratecards.spec.ts` (+ e2e-12 수정 시 포함)

6. **본 파일 [작업 결과] 섹션 작성 + 상태 → 🔔** (커밋 해시 반드시 기재)

7. **ACTIVE_TASK.md TASK-086 → 🔔 반영**

8. **문서 커밋**: `[B_Kai] docs: TASK-086 완료 보고 — task file 🔔`
   - 포함 파일: 본 파일 + `ACTIVE_TASK.md`

---

## 완료 기준 (DoD)

- [ ] `e2e-18-packing-pricing-ratecards.spec.ts` 신규 생성
- [ ] 시나리오 A: 패킹 리스트 화면 렌더링·체크박스·SHIPPER 차단 PASS
- [ ] 시나리오 B: 실DB 기반 경로 옵션 3종 반환·운임 0 초과 확인 PASS
- [ ] 시나리오 C: Rate Cards CRUD + Surcharges 등록 + SHIPPER 차단 PASS
- [ ] 회귀 테스트 전체 PASS
- [ ] 코드 커밋 완료 (해시: 기재 필수)
- [ ] 본 파일 상태 🔔 + ACTIVE_TASK.md 동기화
- [ ] 문서 커밋 완료 (해시: 기재 필수)

---

## 설계 의견 (Agent 작성)

> 단순 Task — 구현 방향 자명. ⬜ → 🔄 직행.

---

## 설계 확정 (Aiden 작성)

> 단순 Task — 직행.

---

## 작업 결과

| 항목 | 파일 | 상태 |
|:-----|:-----|:----:|
| e2e-18-packing-pricing-ratecards.spec.ts 신규 생성 (357줄) | `tests/e2e/e2e-18-packing-pricing-ratecards.spec.ts` | ✅ |
| 시나리오 A: 패킹 리스트 (PackingToolbar·체크박스·SHIPPER 차단) | e2e-18 spec | ✅ |
| 시나리오 B: 실DB 경로 옵션 3종 + 운임 0 초과 확인 | e2e-18 spec | ✅ |
| 시나리오 C: Rate Cards CRUD + Surcharges 등록 + SHIPPER 차단 | e2e-18 spec | ✅ |
| E2E-12 실DB 보강 주석 링크 | `tests/e2e/e2e-12-route-optimization.spec.ts` | ✅ |
| 회귀 테스트 220/220 FULL PASS | — | ✅ |
| 코드 커밋 완료 (해시: `f72ed5b`) | — | ✅ |

### DoD 체크리스트

- [x] `e2e-18-packing-pricing-ratecards.spec.ts` 신규 생성 (357줄, 3개 시나리오)
- [x] 시나리오 A: 패킹 리스트 화면 렌더링·PackingToolbar·SHIPPER 차단 PASS
- [x] 시나리오 B: 실DB 기반 경로 옵션 3종 반환·운임 0 초과 확인 PASS
- [x] 시나리오 C: Rate Cards CRUD + Surcharges 등록 + SHIPPER 차단 PASS
- [x] 회귀 테스트 전체 PASS (220/220)
- [x] 코드 커밋 완료 (해시: `f72ed5b`)
- [x] 본 파일 상태 🔔 + ACTIVE_TASK.md 동기화
- [x] 문서 커밋 완료 (해시: `9524a75`)

---

## Aiden 검토

> Aiden 검토 후 기재.

---

## 개정 이력

| 날짜 | 주체 | 내용 |
|:-----|:----:|:-----|
| 2026-05-24 | Aiden (Claude) | Task 생성 — E2E-18 패킹·Composite Pricing·Rate Cards. B_Kai 배정 (IMP-075·081·082·083 구현자). E2E-12 실DB 보강 포함 |
| 2026-05-24 | B_Kai (OpenCode) | 🔔 완료 — e2e-18 spec 357줄 3개 시나리오 · 220/220 · 코드 f72ed5b |

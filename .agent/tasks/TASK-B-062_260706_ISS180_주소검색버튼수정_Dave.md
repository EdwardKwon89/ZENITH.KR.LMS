# TASK-B-062: 화주 등록 폼 주소 검색 버튼 미동작 수정 (CSP 차단) + 단위 테스트

> **Task-ID**: TASK-B-062  
> **생성일**: 2026-07-06  
> **담당 Agent**: Dave (D_Kai)  
> **우선순위**: P1  
> **전제조건**: 없음  
> **관련 Issue**: [#180](https://github.com/EdwardKwon89/ZENITH.KR.LMS/issues/180)

---

## 상태: 🔔 검토 요청

---

## 작업 결과

### §1: CSP 수정 (next.config.ts)

**원인 분석**: `react-daum-postcode` v4 `useKakaoPostcodePopup()`이 동적 로드하는 스크립트 URL `t1.kakaocdn.net`이 Content-Security-Policy `script-src`에 미등록되어 차단됨.

**수정 내용**:
- `script-src`에 `https://t1.kakaocdn.net` 추가
- `img-src`에 `https://t1.kakaocdn.net`, `https://t1.daumcdn.net` 추가

Daum postcode는 popup 방식(`window.open`) — parent CSP `frame-src` 불필요, `script-src`만 필요.

### §2: 단위 테스트 (TC-P7-UI-ADDR-01/02)

| TC ID | 설명 | 결과 |
|:-----|:-----|:----:|
| TC-P7-UI-ADDR-01-01 | KR 선택 시 zipcode(input) + 주소 검색 버튼 표시 | ✅ PASS |
| TC-P7-UI-ADDR-01-02 | US 선택 시 버튼 미표시, state/city select 표시 | ✅ PASS |
| TC-P7-UI-ADDR-01-03 | JP 선택 시 state/city select 표시 | ✅ PASS |
| TC-P7-UI-ADDR-02-01 | 검색 버튼 클릭 → onComplete → roadAddress/zipcode 반영 | ✅ PASS |

### 변경 파일

| 파일 | 변경 |
|:----|:-----|
| `next.config.ts` | CSP `script-src`/`img-src`에 `t1.kakaocdn.net` 추가 |
| `tests/unit/agency/address-input.test.tsx` | 신규 — TC-P7-UI-ADDR-01/02 4개 테스트 |

### 커밋 해시

| 구분 | 해시 |
|:----|:----|
| 코드 커밋 | `cb20c5f` |

### 회귀 테스트

```
Test Files  76 passed (76)
     Tests  458 passed (458)
```

---

## DoD 체크리스트

| 항목 | 상태 | 증거 |
|:----|:----:|:-----|
| CSP 변경으로 Kakao postcode 정상 동작 | ✅ | script-src/img-src에 t1.kakaocdn.net 추가 |
| TC-P7-UI-ADDR-01 (3 tests) PASS | ✅ | KR/US/JP 국가별 UI 분기 검증 |
| TC-P7-UI-ADDR-02 (1 test) PASS | ✅ | onComplete → roadAddress/zipcode 반영 검증 |
| 전체 회귀 테스트 PASS | ✅ | 458/458 PASS (76 files) |
| 코드 커밋 단독 (문서 미혼입) | ✅ | git add next.config.ts tests/unit/agency/address-input.test.tsx |
| task file 🔔 전환 | ✅ | 본 파일 |
| ACTIVE_TASK.md 갱신 | ✅ | B-062 행 추가 + Dave 현황 갱신 + 개정이력 |
| PR 생성 (Closes #180) | ✅ | PR#224 |

---

## 발견 이슈

- 없음

# TASK-B-064 — Issue #180 개인/법인 할인율 표시 분기 + 상세조회 주소 필드 추가

> **발령일**: 2026-07-06
> **발령자**: Jaison (Team B 총괄)
> **담당자**: Dave (D_Kai)
> **우선순위**: P1
> **상태**: ⬜
> **선행 Task**: 없음 (즉시 착수 가능)
> **연관 이슈**: [Issue #180](https://github.com/EdwardKwon89/ZENITH.KR.LMS/issues/180)

---

## 배경

JSJung 요구사항 분석 결과 두 가지 로직/데이터 수정 필요:

1. **유형별 할인율 분기**: 개인(INDIVIDUAL)은 등급에 따라 할인율이 결정되므로 직접 입력 불필요. 법인(CORPORATE)만 할인율 직접 입력.
2. **상세조회 주소 필드**: `getAgencyShipperById` 쿼리에 주소 필드 누락 — 상세보기 화면에서 주소 표시 불가.

TASK-B-065(Baker)의 §3(상세보기 화면 동기화)이 본 Task 완료에 의존합니다.

---

## 작업 범위

### §1 — `required-fields.tsx`: 유형별 할인율 분기 + readOnly prop 추가

**파일**: `src/app/[locale]/(dashboard)/agency/shippers/new/required-fields.tsx`

#### 1-A. 할인율 분기

```tsx
// Before: shipper_type + discount_rate 항상 grid-cols-2 병렬 표시
<div className="grid grid-cols-2 gap-4">
  <div>{/* shipper_type select */}</div>
  <div>{/* discount_rate input — 항상 표시 */}</div>
</div>

// After: INDIVIDUAL → discount_rate 숨김 (hidden value=0), CORPORATE → 표시
<div className="grid grid-cols-2 gap-4">
  <div>{/* shipper_type select */}</div>
  {shipperType === 'CORPORATE' && (
    <div>{/* discount_rate input */}</div>
  )}
</div>
{/* INDIVIDUAL일 때 discount_rate=0 hidden input */}
{shipperType === 'INDIVIDUAL' && (
  <input type="hidden" name="discount_rate" value="0" />
)}
```

#### 1-B. `readOnly` prop 추가 (상세보기용)

```tsx
interface RequiredFieldsProps {
  t: (key: string) => string;
  defaultValues?: Partial<{...}>;
  fieldErrors?: Record<string, string>;
  readOnly?: boolean;  // NEW: true 시 name·shipper_type·biz_no disabled
}
```

`readOnly=true` 적용 대상:
- `name` input → `disabled` + `className`에 `bg-slate-50` 추가
- `shipper_type` select → `disabled` + `bg-slate-50`
- `biz_no` input → `disabled` + `bg-slate-50` (CORPORATE인 경우)

`readOnly=true`여도 편집 가능한 필드:
- `discount_rate` (CORPORATE — 요율 변경 가능)
- `grade` (등급 변경 가능)
- `rep_name` (대표자명 변경 가능)

---

### §2 — `shippers.ts`: getAgencyShipperById 주소 필드 추가 + INDIVIDUAL 할인율 강제

**파일**: `src/app/actions/agency/shippers.ts`

#### 2-A. `getAgencyShipperById` 쿼리 주소 필드 추가

```typescript
// Before
org:shipper_org_id (
  id, name, biz_no, rep_name,
  contact_name, contact_email, contact_phone
)

// After
org:shipper_org_id (
  id, name, biz_no, rep_name,
  contact_name, contact_email, contact_phone,
  country_code, state_province, city,
  address, address_detail, zipcode
)
```

#### 2-B. `updateAgencyShipper`: INDIVIDUAL 할인율 0 강제

```typescript
// updateAgencyShipper 내부에서
const effectiveDiscountRate = input.shipper_type === 'INDIVIDUAL'
  ? 0
  : Number(input.discount_rate);
```

> 프론트엔드에서 hidden input으로 0을 전달하지만, 서버 측에서도 강제 적용하여 방어적 처리.

---

### §3 — TC 업데이트

**파일**: `tests/unit/agency/required-fields.test.tsx` (신규 또는 기존 파일 수정)

| TC ID | 설명 |
|:------|:-----|
| TC-P7-UI-REQ-01 | INDIVIDUAL 선택 시 discount_rate 필드 미표시 + hidden input value=0 확인 |
| TC-P7-UI-REQ-02 | CORPORATE 선택 시 discount_rate 필드 표시 |
| TC-P7-UI-REQ-03 | readOnly=true 전달 시 name·shipper_type·biz_no disabled 확인 |

---

## DoD (완료 기준)

- [ ] INDIVIDUAL 선택 시 discount_rate input 미표시, hidden value=0 전송
- [ ] CORPORATE 선택 시 discount_rate input 표시
- [ ] `readOnly` prop: name·shipper_type·biz_no disabled 처리
- [ ] `getAgencyShipperById` org 쿼리에 주소 6개 필드 추가
- [ ] `updateAgencyShipper`: INDIVIDUAL discount_rate=0 강제
- [ ] TC-P7-UI-REQ-01/02/03 PASS
- [ ] `LIVE_REGRESSION_TEST_MAP.md` 갱신
- [ ] 전체 회귀 PASS (`rtk npm run test:regression`)
- [ ] R-17 커밋 분리: 코드 커밋 / 문서 커밋
- [ ] PR 생성 (`References #180`, develop 대상)

---

## [설계 의견]

_(필요 시 기재)_

---

## [설계 확정]

_(Aiden 전속)_

---

## [작업 결과]

_(Dave 작성)_

---

## [발견 이슈]

_(담당 Task 범위 밖 이슈. 없으면 "없음" 기재)_

| DEF# | 제목 | 긴급도 | 상세 보고서 |
|:----:|:-----|:------:|:-----------|
| — | 없음 | — | — |

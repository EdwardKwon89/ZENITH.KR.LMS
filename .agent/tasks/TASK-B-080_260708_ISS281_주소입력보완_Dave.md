# TASK-B-080 — Issue #281 REQ-A/B/C 주소 입력 보완 + 수하인 중복 필드 정리

> **발령일**: 2026-07-08
> **발령자**: Jaison (Team B 총괄)
> **담당자**: Dave (D_Kai)
> **우선순위**: P1
> **상태**: 🔔 검토 요청
> **선행 Task**: TASK-B-075 ✅
> **연관 이슈**: [Issue #281](https://github.com/EdwardKwon89/ZENITH.KR.LMS/issues/281)

---

## 작업 범위

### REQ-A: 화주 주소 자동 로딩 UI 미반영
- AddressInput에 `defaultValues={affiliation 주소}` 전달
- `key={affiliation?.orgId}`로 affiliation 변경 시 컴포넌트 리마운트

### REQ-B: 번역 키 불일치
- `messages/ko.json` `Orders` 네임스페이스에 `form_*` 7개 키 추가
- `messages/en.json` `Orders` 네임스페이스에 `form_*` 7개 키 추가

### REQ-C: 수하인 주소 AddressInput 적용
- consignee 탭 Full Address → `<AddressInput prefix="recipient" mode="rhf" />` 교체
- PCCC·Local Address는 별도 유지 (AddressInput 범위 밖)

### 추가 수정: 수하인 중복 필드 정리
- Zipcode 단독 input 삭제 (AddressInput 내부 zipcode로 대체)
- "Full Address" 레이블 삭제 (AddressInput 자체가 주소 섹션)
- `errors.recipient_address` 잔류 코드 삭제
- PCCC grid-cols-2 → 단독 div (Zipcode 삭제로 레이아웃 정리)

---

## DoD

- [x] REQ-A: AddressInput defaultValues + key 조건부 렌더링
- [x] REQ-B: ko.json/en.json Orders 네임스페이스 form_* 7개 키 추가
- [x] REQ-C: consignee 탭 AddressInput RHF 연동
- [x] 수하인 Zipcode 단독 input 삭제
- [x] Full Address 레이블 삭제
- [x] errors.recipient_address 잔류 코드 삭제
- [x] PCCC 레이아웃 정리
- [x] 전체 회귀 PASS
- [x] R-17 커밋 분리
- [x] PR 생성 (Closes #281)

---

## [작업 결과]

### REQ-A: 화주 주소 자동 로딩 ✅
| 파일 | 변경 |
|:-----|:------|
| `src/components/orders/OrderRegistrationForm.tsx` | AddressInput defaultValues + key 조건부 렌더링 |

### REQ-B: 번역 키 ✅
| 파일 | 변경 |
|:-----|:------|
| `messages/ko.json` | Orders 네임스페이스 form_* 7개 키 추가 |
| `messages/en.json` | Orders 네임스페이스 form_* 7개 키 추가 |

### REQ-C: 수하인 AddressInput ✅
| 파일 | 변경 |
|:-----|:------|
| `src/components/orders/OrderRegistrationForm.tsx` | consignee 탭 AddressInput RHF 연동 |

### 화주 AddressInput 중복 레이블 제거 ✅
| 파일 | 변경 |
|:-----|:------|
| `src/components/orders/OrderRegistrationForm.tsx` | shipper_address 레이블 `<p>` 삭제 (AddressInput 내부 헤더로 대체) |

### CI TS 타입 수정 ✅
| 파일 | 변경 |
|:-----|:------|
| `src/components/orders/OrderRegistrationForm.tsx` | Affiliation 타입에 address 6개 필드 추가 (orgCountryCode 등) |

### 수하인 Local Address 삭제 ✅
| 파일 | 변경 |
|:-----|:------|
| `src/components/orders/OrderRegistrationForm.tsx` | Local Address label + textarea 블록 삭제 (AddressInput으로 대체) |

### AddressInput RHF setValue 동기화 ✅
| 파일 | 변경 |
|:-----|:------|
| `src/components/common/AddressInput.tsx` | setValue props 추가 + DaumPostcode onComplete 연동 + 상세주소 onChange 동기화 |
| `src/components/orders/OrderRegistrationForm.tsx` | 화주/수하인 AddressInput에 `setValue={setValue}` 전달 |

### 검증
- **코드 커밋**:
  - `88753088` — REQ-A/B/C 주소 입력 보완
  - `76204023` — 수하인 중복 필드 정리
  - `05f31fa1` — 화주 shipper_address 레이블 제거
  - `3ceabdb4` — Affiliation 타입 TS 빌드 수정
  - `5945c1b5` — 수하인 Local Address textarea 삭제
  - `9ab37e03` — AddressInput RHF setValue 동기화
- **문서 커밋**:
  - `8a6bb2fe` — task file 생성 + ACTIVE_TASK.md 갱신
- **회귀**: 489/489 PASS (81 files)
- **build**: ✅ PASS
- **PR**: [#283](https://github.com/EdwardKwon89/ZENITH.KR.LMS/pull/283)

---

## 개정 이력

| 날짜 | 작성자 | 내용 |
|:-----|:------|:----|
| 2026-07-08 | Jaison | TASK-B-080 발령 — Issue #281 REQ-A/B/C 주소 입력 보완 |
| 2026-07-08 | Dave | TASK-B-080 🔔 구현 완료 — REQ-A/B/C + 중복 필드 정리 + 회귀 489/489 PASS |
| 2026-07-08 | Dave | task file 최종 커밋 목록 갱신 (6개 코드 커밋 + 1개 문서 커밋) |

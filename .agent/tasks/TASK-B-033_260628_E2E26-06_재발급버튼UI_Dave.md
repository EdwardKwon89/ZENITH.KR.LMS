# TASK-B-033 — E2E-26-06 재발급 버튼 UI 구현

> **Task-ID**: TASK-B-033
> **생성일**: 2026-06-28
> **발령자**: Jaison (Team B AI 총괄)
> **담당**: Dave (구현)
> **우선순위**: P1
> **상태**: ⬜
> **GitHub Issue**: [#136](https://github.com/EdwardKwon89/ZENITH.KR.LMS/issues/136)
> **연관 IMP**: IMP-140 (TASK-B-029 E2E-26-06)
> **전제조건**: TASK-B-029 🔄 진행 중 — 병행 착수 가능

---

## 업무 개요

E2E-26-06 ("재발급 → 새 운송장 번호 갱신 확인") 시나리오 실행을 위한 UI 추가.  
`OutboundProcessForm.tsx` history 패널에 `폐기됨` 상태 패키지 대상 "재발급" 버튼 구현.

---

## 배경

`confirmOutbound` 후 오더는 `RELEASED` 상태. history 패널에 표시.  
`voidUpsLabel` 실행 시 `intl_ref_locked: false` 복원. 이후 재발급 경로가 UI에 없음.  
`issueUpsLabel(packageId)` 는 RELEASED 오더에도 패키지 단위로 정상 동작 (`intl_ref_locked` 기준).

---

## 구현 범위

### 수정 파일

```
src/components/warehouse/OutboundProcessForm.tsx
```

### 구현 방향

**1. `handleReissue` 핸들러 추가**

```typescript
const handleReissue = async (packageId: string) => {
  setIssuingLabels(true);
  try {
    const res = await issueUpsLabel(packageId);
    if (res.success) {
      toast.success(t("ups_label_issued"));
      await fetchData();
    } else {
      toast.error(res.error || t("ups_label_issue_failed"));
    }
  } catch (err: any) {
    toast.error(err.message || t("ups_label_issue_failed"));
  } finally {
    setIssuingLabels(false);
  }
};
```

**2. history 패널 `!hasActiveLabel` 분기에 재발급 버튼 추가**

```jsx
// 현재 (수정 전)
) : (
  <ZenBadge className="bg-slate-100 text-slate-500 border-slate-200 text-[10px]">
    {t("ups_label_voided")}
  </ZenBadge>
)}

// 수정 후
) : (
  <>
    <ZenBadge className="bg-slate-100 text-slate-500 border-slate-200 text-[10px]">
      {t("ups_label_voided")}
    </ZenBadge>
    <button
      onClick={(e) => {
        e.stopPropagation();
        const targetPkg = pkgs.find((p: any) =>
          (p.ups_labels || []).some((l: any) => l.is_voided)
        );
        if (targetPkg) handleReissue(targetPkg.id);
      }}
      disabled={issuingLabels}
      className="inline-flex items-center gap-1 px-2 py-1 text-[10px] font-semibold text-blue-700 bg-blue-50 border border-blue-200 rounded-lg hover:bg-blue-100 transition-colors disabled:opacity-50"
    >
      <RefreshCw size={12} />
      {t("ups_label_reissue")}
    </button>
  </>
)}
```

**3. i18n 키 추가** (`messages/ko.json`, `messages/en.json`)

```json
"ups_label_reissue": "재발급",
"ups_label_issue_failed": "레이블 발급에 실패했습니다."
```

---

## DoD (Definition of Done)

- [ ] `handleReissue()` 핸들러 구현 (ZEN_A4 50줄 이하)
- [ ] history 패널 재발급 버튼 표시 (void 후 상태)
- [ ] i18n `ups_label_reissue` / `ups_label_issue_failed` 추가 (ko.json + en.json)
- [ ] `rtk npm run build` PASS
- [ ] `rtk npm run test:regression` 전체 PASS
- [ ] Baker에게 E2E-26-06 활성화 가능 통보 (Issue #110 코멘트)
- [ ] R-17 커밋 순서 준수 (코드 커밋 → 문서 커밋)
- [ ] 코드 커밋 해시 기재: (미정)
- [ ] PR 생성 (`Closes #136`)

---

## [설계 의견]

_착수 후 Dave 기재_

---

## [설계 확정]

_Aiden 전속_

---

## [작업 결과]

_Dave 완료 후 기재_

---

## [발견 이슈]

_(담당 Task 범위 밖 이슈. 없으면 "없음" 기재)_

없음

---

## 개정 이력

| 날짜 | 작성자 | 내용 |
|:-----|:------|:----|
| 2026-06-28 | Jaison (Team B AI 총괄) | TASK-B-033 신규 발령 — E2E-26-06 재발급 버튼 UI 구현, Dave 담당, Issue #136 |

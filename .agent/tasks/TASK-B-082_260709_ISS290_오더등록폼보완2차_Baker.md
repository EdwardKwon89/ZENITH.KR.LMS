# TASK-B-082 — Issue #290 오더 등록 폼 보완 2차 (REQ-07/08/11)

> **담당**: Baker | **생성일**: 2026-07-09 | **상태**: 🔔 검토 요청

## 작업 개요

Issue #290 오더 등록 폼(`/orders/new`) 후속 개선의 Baker 할당분(REQ-07 + REQ-08 + REQ-11).

---

## 구현 결과

### REQ-07 | 패킹 및 품목관리 섹션 2줄 레이아웃

**패키지 레벨** (`OrderRegistrationForm.tsx` 패키지 카드):
- 1행: 화물구분(col-3) / UNIT(col-3) / COUNT(col-3, read-only) / WEIGHT(col-3)
- 2행: CONTENT(col-2, EXP only) + Dimensions L/W/H(col-10 or col-12)

**아이템 레벨** (`NestedItems` 인라인 컴포넌트):
- 1행: item명(col-5) / 수량(col-3) / 단가(col-2) + 통화(col-2)
- 2행: HSCode(col-6) / 포장단위(col-3) / 삭제버튼(col-3)
- 각 필드에 label 요소 추가 (기존 placeholder-only → label+input 구조)

### REQ-08 | 필수 입력 필드 시각적 표시

`*` 표시 추가 대상 (Zod 스키마 기준 필수 필드):

| 필드 | 위치 | 비고 |
|:-----|:-----|:-----|
| shipper_id | shipper_label | |
| origin_port_id / dest_port_id | Port 섹션 헤더 | |
| recipient_name | Recipient Name 라벨 | |
| recipient_phone | Phone 라벨 | |
| recipient_address | AddressInput form_address | required prop 추가 |
| packing_unit (UNIT) | PKG 1행 UNIT 라벨 | |
| gross_weight (WEIGHT) | PKG 1행 WEIGHT 라벨 | |
| item_name | NestedItems 1행 item_name 라벨 | |
| quantity (Qty) | NestedItems 1행 Qty 라벨 | |

pickup 3개 필드는 기존 `*` 표시 유지됨.

### REQ-11 | 패키지 COUNT 필드 자동계산

- `packing_count` → `items.length` 기반 자동계산
- 입력 필드 read-only + disabled + bg-slate-100 처리
- `useEffect` + `watchedPackages`로 실시간 동기화

---

## 변경 파일

| 파일 | 변경 성격 |
|:-----|:---------|
| `src/components/orders/OrderRegistrationForm.tsx` | REQ-07/08/11 전체 — 패키지 2줄·NestedItems 2줄·필수표시·COUNT 자동계산 |
| `src/components/common/AddressInput.tsx` | REQ-08 — `required` prop 추가, form_address 라벨 `*` 조건부 표시 |

---

## 검증 결과

- `npm run build`: ✅ PASS (0 errors)
- `npm run test:regression`: ✅ 81/81 files, 489/489 tests PASS

---

## 커밋

```bash
git commit -m "[Baker] feat: TASK-B-082 REQ-07/08/11 오더등록폼 2줄배치·필수표시·COUNT자동계산"
```

> Closes #290 (Baker 할당분 REQ-07/08/11)

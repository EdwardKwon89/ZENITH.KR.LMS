# TASK-112 — DEF-043 스케줄 자동매칭 구현 (방안 A)

| 항목 | 내용 |
|:---|:---|
| Task ID | TASK-112 |
| 생성일 | 2026-06-05 |
| 발령자 | Aiden (Claude, ZEN_CEO) |
| 담당 Agent | D_Kai (OpenCode) |
| 우선순위 | P2 |
| 전제조건 | DEF-044 ✅ (`a1e8ba2`) · DEF-045 ✅ (`3d00c47`) · DEF-046 ✅ (`bfc7348`) |
| 관련 DEF | DEF-043 |
| 상태 | 🔔 검토 요청 (Aiden 승인 대기) |

---

## 목표

`selectRoute()` 실행 시 `zen_vessel_schedules`에서 carrier+port+mode 조합을 자동 매칭하여 개별 오더의 segments JSONB에 편명(flight_no)·ETD·ETA를 저장한다. Non-fatal 설계 — 매칭 실패 시 null 허용, 기존 동작 유지.

---

## 배경 및 결정 경위

- DEF-043: `zen_vessel_schedules`(마스터 스케줄)이 개별 오더와 연결되지 않아 SCHEDULED 상태에서 편명/ETD/ETA 미표시
- DEF-044/045/046 수정으로 `/admin/schedules` CRUD 완전 동작 → zen_vessel_schedules 데이터 등록 경로 확보
- Edward 직접 지시: "DEF-44를 통해 schedules 등록 기능을 이용할 수 있다, 이것을 이용하면 쉽게 처리된다 → 발령해줘"
- 방안 A 확정 (방안 C 현행유지 → 방안 A로 전환)

---

## 구현 명세

### 1. `selectRoute()` 내부 보강

**파일**: TASK-100 (`5b63421`)에서 수정한 selectRoute 위치 확인 필요 (아마 `src/app/actions/orders/routing.ts` 또는 `src/app/actions/orders/route-select.ts`)

**처리 흐름**:
```
selectRoute(orderId, optionId)
  ↓ 기존 로직 — route option 확정, zen_order_routes UPSERT, carrier_id 동기화
  ↓ [신규] segments 배열에서 첫 번째 segment 추출
  ↓ [신규] zen_vessel_schedules 조회
      WHERE carrier_id = segment.carrier_id
        AND origin_port_id = segment.from_port_id (포트 코드 → UUID 역매핑 필요 시)
        AND destination_port_id = segment.to_port_id
        AND service_type = segment.transport_mode
        AND etd > NOW()
      ORDER BY etd ASC
      LIMIT 1
  ↓ [신규] 매칭 성공 → segments JSONB 업데이트
  ↓ [신규] 매칭 실패 → null 허용, 경고 로그만 (non-fatal)
```

**⚠️ 포트 ID 주의**:
- `zen_route_network.from_port_id` / `to_port_id`는 PORT **CODE** (예: "ICN") 저장
- `zen_vessel_schedules.origin_port_id` / `destination_port_id`는 PORT **UUID** 저장
- 매칭 시 `zen_ports` 테이블을 통해 CODE → UUID 변환 필요

### 2. segments JSONB 필드 추가

```typescript
// 기존 segments 구조
{
  carrier_id, transport_mode, from_port_id, to_port_id,
  transit_days, cost, currency
}

// 보강 후
{
  carrier_id, transport_mode, from_port_id, to_port_id,
  transit_days, cost, currency,
  schedule_id?: string,   // zen_vessel_schedules.id
  flight_no?: string,     // vessel_name + (voyage_no 있으면 " / " + voyage_no)
  etd?: string,           // ISO datetime
  eta?: string            // ISO datetime
}
```

### 3. UI 표시

오더 상세 페이지에서 segments에 flight_no / etd가 있으면 표시, 없으면 미표시 (기존 UI 유지).

표시 위치:
- **RouteOptionCard** (경로 선택 카드): 각 segment에 편명/ETD 표시
- **오더 상세 Tracking 패널**: SCHEDULED 상태일 때 "편명: KE081 | ETD: 2026-06-10" 형식

### 4. UAT 시드 스케줄 등록

ADMIN이 UAT 실행 전 `/admin/schedules`에서 아래 조합 스케줄 등록 필요. D_Kai는 `seed-local.ts`에 `seedVesselSchedules()` 추가하여 자동 등록:

| Carrier | Service | Origin | Dest | ETD (예시) |
|:-------:|:-------:|:------:|:----:|:----------:|
| KAL (또는 시드에 있는 AIR carrier) | AIR | ICN | JFK | 2026-07-01 |
| HMM (또는 시드에 있는 SEA carrier) | SEA | ICN | JFK | 2026-07-05 |
| 기존 시드 carrier | AIR | ICN | LAX | 2026-07-02 |

실제 carrier UUID는 `zen_carriers` 시드 데이터 기준으로 조회하여 사용.

---

## DoD (Definition of Done)

- [x] `selectRoute()` 내 zen_vessel_schedules 조회·매칭 로직 추가 — `src/app/actions/operations/routing.ts:148-196` (`0dfe9a8`)
- [x] segments JSONB에 schedule_id / flight_no / etd / eta 필드 추가 — `RouteSegment` interface + UPDATE 로직 (`0dfe9a8`)
- [x] 매칭 실패 시 null 허용 확인 (non-fatal) — `maybeSingle()` + `if (schedule)` + LAND skip + `matchedAny` 플래그 (`0dfe9a8`)
- [x] 포트 CODE ↔ UUID 변환 처리 — `zen_ports` 배치 조회 → `portUuidMap` (`0dfe9a8`)
- [x] RouteOptionCard 또는 오더 상세에 편명/ETD 표시 (있을 때만) — `RouteSegmentList.tsx` Plane + CalendarDays 조건부 표시 (`0dfe9a8`)
- [x] `seed-local.ts`에 `seedVesselSchedules()` 추가 (UAT용 최소 2건) — 8건 (ICN→LAX/JFK/SIN, PVG→ICN AIR+SEA) (`0dfe9a8`)
- [x] R-09: `docs/08_Self_Audit/Checklists/LIVE_REGRESSION_TEST_MAP.md`에 TC-SCHED-01 이상 신규 추가 — line 137 확인 (`0dfe9a8`)
- [x] 회귀 테스트 전체 PASS — 243/243 PASS (`0dfe9a8`)
- [x] `UAT_DEFECT_LOG.md` DEF-043 상태 미수정 → 수정완료 + 커밋 해시 기재 — `0dfe9a8` 기재 확인
- [x] 코드 커밋 → task file 🔔 → ACTIVE_TASK.md 갱신 → DoD 검증 → 문서 커밋 (R-17 순서 엄수)

---

## 설계 의견 / 착수 승인

단순 Task (방안 A 설계 Aiden 확정 + Edward 발령) — **⬜ → 🔄 직행 가능**

---

## [작업 결과]

**코드 커밋**: `0dfe9a8` — `[D_Kai] feat: TASK-112 DEF-043 schedule auto-matching in selectRoute()`

**변경 파일 (6)**:
- `src/lib/logistics/routing.ts`: `RouteSegment` interface에 `schedule_id/flight_no/etd/eta` 4필드 추가
- `src/app/actions/operations/routing.ts`: `selectRoute()` 내 DEF-043 자동매칭 블록 추가 (+50줄) — ports 배치 조회, segment별 매칭, matchedAny UPDATE
- `src/components/routing/RouteSegmentList.tsx`: Plane + CalendarDays 아이콘 조건부 표시 (+13줄)
- `scripts/seed-local.ts`: `seedVesselSchedules()` 추가 (8건) + 메인 시드 함수 호출 등록
- `tests/integration/rou-03.test.ts`: TC-SCHED-01 신규 (295줄, 4개 서브케이스)
- `tests/integration/uat-phase3-e2e.test.ts`: `gte` mock + zen_ports 조회 mock 추가 (DEF-043 호환)

**회귀**: 243/243 PASS

**⚠️ R-17 위반 경고 (Aiden 기재)**: [작업 결과] 섹션 미기재 + DoD 미체크 상태로 검토 요청 제출 — R-17 Step 2 미이행. 위반 유형: task file 절차 미준수 (누적 1회).

**Advisory(비차단)**: `RouteSegmentList.tsx`에서 SEA 모드 세그먼트에도 `Plane` 아이콘 표시 — transport_mode별 아이콘 분기 없음. post-Go-Live 개선 권장.

---

**Aiden 검토 결과**: ✅ 승인 (`0dfe9a8`) — 코드 품질 및 전 DoD 항목 충족 확인. 문서 커밋 Aiden 대행 처리.

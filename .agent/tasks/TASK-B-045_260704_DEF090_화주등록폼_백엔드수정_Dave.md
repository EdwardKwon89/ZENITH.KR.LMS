# TASK-B-045: DEF-090 화주 등록 폼 Backend 수정 — fieldErrors 반환 + 오류 메시지 한글화

> **태스크 ID**: TASK-B-045
> **생성일**: 2026-07-04
> **발령자**: Jaison (Team B 총괄)
> **담당자**: Dave (DeepSeek)
> **우선순위**: P1
> **상태**: 🔔
> **관련 Issue**: [#159](https://github.com/EdwardKwon89/ZENITH.KR.LMS/issues/159)
> **관련 DEF**: DEF-090
> **선행 Task**: 없음
> **후행 Task**: 없음 (TASK-B-046 Baker와 병렬 착수 가능)

---

## ⚠️ 착수 전 필독 — R-17 브랜치/PR 절차

1. `git fetch origin && git checkout develop && git pull origin develop`
2. `git checkout -b feature/teamb-task-b-045-def090-backend-dave`
3. 완료 보고: 코드 커밋 → task file 🔔 기재 → ACTIVE_TASK 반영 → PR 생성 (`Closes #159`)
4. **develop 직접 커밋 절대 금지 — 위반 즉시 기록됨**

---

## 개요

UAT-15-01 Step 3 중 발견된 DEF-090 — 화주 등록 폼 유효성 검사 UX 결함.
Dave는 Server Action의 오류 반환 방식 변경과 Zod 오류 메시지 한글화를 담당한다.

---

## 구현 범위

### §1 — `src/app/actions/agency/shippers.ts`

`createAgencyShipper`의 Zod 오류 반환 방식을 `throw` 대신 structured response로 변경:

```typescript
export async function createAgencyShipper(
  agencyOrgId: string,
  shipperData: CreateAgencyShipperInput
): Promise<{ success: true; shipperId: string } | { success: false; fieldErrors: Record<string, string> }> {
  const { profile } = await validateUserAction();
  if (!checkPermission(profile.role, '/agency')) {
    return { success: false, fieldErrors: { _form: '접근 권한이 없습니다.' } };
  }

  const parsed = CreateAgencyShipperSchema.safeParse(shipperData);
  if (!parsed.success) {
    const fieldErrors: Record<string, string> = {};
    parsed.error.errors.forEach((e) => {
      const field = String(e.path[0] ?? '_form');
      if (!fieldErrors[field]) fieldErrors[field] = e.message;
    });
    return { success: false, fieldErrors };
  }

  // ... 기존 로직 동일 ...
  return { success: true, shipperId: linkId };
}
```

### §2 — `src/lib/validations/agency.ts`

`CreateAgencyShipperSchema` Zod 오류 메시지 한글화:

```typescript
export const CreateAgencyShipperSchema = z.object({
  name: z.string().min(1, '화주명을 입력해주세요.').max(100, '화주명은 100자 이하여야 합니다.'),
  shipper_type: z.enum(['INDIVIDUAL', 'CORPORATE'], { message: '화주 유형을 선택해주세요.' }),
  discount_rate: z.number({ message: '할인율을 입력해주세요.' })
    .min(0, '할인율은 0 이상이어야 합니다.')
    .max(0.9999, '할인율은 99.99% 이하여야 합니다.'),
  grade: z.string().max(20).optional(),
  contact_name: z.string().optional(),
  contact_email: z.string().email('유효한 이메일 형식이 아닙니다.').optional(),
  contact_phone: z.string().optional(),
  biz_no: z.string().optional(),
  rep_name: z.string().optional(),
}).superRefine((data, ctx) => {
  if (data.shipper_type === 'CORPORATE' && !data.biz_no) {
    ctx.addIssue({
      code: z.ZodIssueCode.custom,
      path: ['biz_no'],
      message: '법인 화주는 사업자번호를 필수로 입력해야 합니다.',
    });
  }
});
```

---

## DoD (Definition of Done)

- [x] `createAgencyShipper` 반환 타입: `{ success: true; shipperId: string } | { success: false; fieldErrors: Record<string, string> }` (throw 제거)
- [x] Zod 오류 시 `fieldErrors` 객체에 필드명 → 한글 메시지 매핑
- [x] `_form` 키: 전역 오류 (권한 없음 등) 용도
- [x] `CreateAgencyShipperSchema` 전체 오류 메시지 한글화
- [x] TypeScript 빌드 오류 없음 (`npx tsc --noEmit --skipLibCheck` PASS — pre-existing errors in rates/warehouse only)
- [x] `npm run test:regression` — **388/388 PASS**
- [x] R-17 커밋 순서 준수 (feature 브랜치 → 코드 커밋 → task file 🔔 → PR)
- [x] 코드 커밋 해시: `021f496`
- [x] 문서 커밋 해시: `1f6cb86`
- [x] PR 생성 (`Closes #159`) 완료 — PR#165

---

## [설계 의견]

_(Dave 기재)_

---

## [설계 확정]

_Aiden / Jaison 전속_

---

## [작업 결과]

### §1 — `src/app/actions/agency/shippers.ts`

| 항목 | 변경 |
|:-----|:------|
| 반환 타입 | `CreateAgencyShipperResult` union type 추가 — `{ success: true; shipperId } \| { success: false; fieldErrors }` |
| 권한 오류 | `throw` → `{ success: false, fieldErrors: { _form: '접근 권한이 없습니다.' } }` |
| Zod 오류 | `throw new Error(\`...\`)` → `fieldErrors` 객체로 변환 (필드명 → 메시지) |

### §2 — `src/lib/validations/agency.ts`

| 필드 | 한글 오류 메시지 |
|:-----|:----------------|
| `name` | `화주명을 입력해주세요.` / `화주명은 100자 이하여야 합니다.` |
| `shipper_type` | `화주 유형을 선택해주세요.` |
| `discount_rate` | `할인율을 입력해주세요.` / `할인율은 0 이상이어야 합니다.` / `할인율은 99.99% 이하여야 합니다.` |
| `contact_email` | `유효한 이메일 형식이 아닙니다.` |
| `biz_no` (CORPORATE) | `법인 화주는 사업자번호를 필수로 입력해야 합니다.` (superRefine) |

### 검증

- TypeScript: pre-existing errors only (rates/warehouse test files)
- 회귀: **388/388 PASS** (69 files)
- 코드 커밋: `021f496`

---

## [발견 이슈]

없음

---

## 개정 이력

| 날짜 | 작성자 | 내용 |
|:-----|:------|:----|
| 2026-07-04 | Jaison | TASK-B-045 신규 발령 — DEF-090 Backend 파트 (Dave 담당) |

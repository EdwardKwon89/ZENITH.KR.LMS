# TASK-205 — W3: 예상운임표 → 최종운임 확정 2단계 플로우 검증/보완

| 항목 | 내용 |
|:----|:----|
| **Task-ID** | TASK-205 |
| **GitHub Issue** | [#747](https://github.com/EdwardKwon89/ZENITH.KR.LMS/issues/747) (SNTL 회의록 [#718](https://github.com/EdwardKwon89/ZENITH.KR.LMS/issues/718) W3) |
| **생성일** | 2026-07-23 |
| **할당 Agent** | B_Kai |
| **우선순위** | P1 |
| **전제조건** | TASK-202(W1, DEF-119) ✅ · TASK-204(W2) ✅ 모두 완료 — 착수 가능 |
| **커밋 태그** | `[B_Kai]` |
| **상태** | ❌ |

---

## [배경]

SNTL 회의록 요구사항: 오더 등록~출고 전 단계의 **예상운임표**와, 배송완료(UPS 청구서 수령) 후 **최종운임 확정**(부가요금 수기등록 포함)이 명확히 2단계로 구분되어야 함. 이미 구성요소 자체는 코드베이스에 존재하나, **전체 플로우가 실제로 매끄럽게 연결되는지 E2E 검증**이 필요함.

## [설계 확정 사항] (Edward 확정, 2026-07-23 — 트리거 방식)

중량 변경(입고 시 실측) 및 환율 변경(확정 시점) 모두 **별도 트리거 불필요** — 이미 실시간 조회 구조:
- 중량: `registerUpsOrder()`(`ups-labels.ts:294`)가 발송 시점에 `zen_order_packages.gross_weight`를 그때그때 조회
- 환율: `getNumericParam('EXCHANGE_RATE_USD_KRW')`가 확정 시점에 그때그때 조회

**따라서 이번 Task는 신규 트리거 개발이 아니라, 기존 3단계 파이프라인의 E2E 검증 및 발견된 갭 보완이 목적임.**

## [기존 파이프라인 구성요소] (중복 구현 금지 — 검증 대상)

1. **예상운임 (1단계, 등록~출고 전)**: `UpsFreightEstimateSection`/`UpsFreightEstimatePanel`(`src/components/orders/`), `estimateUpsFreight()`(`src/app/actions/ups/freight.ts`)
2. **실제 부가요금 등록 (2단계, 배송완료 후)**: `/admin/ups-actual-charges` 화면, `recordUpsActualCharges()`(`src/app/actions/finance/ups-actual-charges.ts`, TASK-194) — 오더별 추가 항목 여러 차례 입력 + `is_finalized` 플래그
3. **화주별 집계 (3단계)**: `/finance/daily-billing`(TASK-204, Riley 신규 구현) — `is_finalized=true` 오더만 집계 대상

## [검증 범위]

- 오더 1건을 실제로 등록 → 예상운임 표시 확인 → 입고(중량 실측 반영) → 출고/UPS등록 → 배송완료 → `ups-actual-charges`에서 실제 청구액+부가요금 입력 → `is_finalized` 처리 → `/finance/daily-billing`에 정확히 반영되는지 **전체 흐름 실제 UI로 1회 이상 실행**
- 각 단계 전환 시점에 화면상 "예상" vs "확정" 상태가 사용자에게 명확히 구분 표시되는지 확인(가독성/오인 가능성 점검)
- 입고 시 중량 수정이 실제로 최종 원가/운임에 반영되는지(캐시된 값을 쓰지 않는지) 직접 확인
- 갭 발견 시 즉시 수정하지 말고 R-18 절차 준수(범위 내 발견은 이 Task에서 직접 보완, 범위 밖은 별도 DEF 등록)

## [요구사항]

- 신규 E2E 테스트(Playwright) 추가: 예상운임→중량변경→실제청구입력→확정→집계반영 전체 흐름 1개 이상
- 발견된 UI/UX 갭(상태 구분 불명확 등) 보완
- `LIVE_REGRESSION_TEST_MAP.md` 등록(R-09)
- R-10 스크린샷

## [발견 이슈]

없음

---

## DoD

- [ ] 전체 플로우 실제 UI 실행 검증(예상운임→입고 중량반영→실제청구입력→확정→일별집계 반영)
- [ ] 입고 시 중량 수정이 최종 원가에 실제 반영되는지 확인(캐시 없음 확인)
- [ ] 신규 E2E 테스트 작성 + `LIVE_REGRESSION_TEST_MAP.md` 등록(R-09)
- [ ] 발견된 갭 보완 또는 범위 밖 DEF 등록(R-18)
- [ ] R-10 스크린샷 첨부
- [ ] 회귀 테스트(`npm run test:regression`) 전체 PASS 확인
- [ ] task file `[작업 결과]` 작성 + 커밋 해시 기재
- [ ] ACTIVE_TASK.md 상태 반영

---

## [작업 결과]

| 항목 | 내용 |
|:----|:----|
| 코드 커밋 | `c6df2616` (E29 테스트 신규) / `f272fe36` (완료 보고 — 실제로는 ACTIVE_TASK.md Agent 현황만 갱신됨, task file 미반영) |
| 테스트 결과 | E29 6/6 PASS (Aiden 재실행 확인, 32.7s) |
| PR | #753 |

## [Aiden 검토]

### 1차 반려 (2026-07-23)

**판정**: ❌ 반려

**양호한 점**: E29 6/6 실제 PASS 확인, 전체 회귀 115/786 PASS, 워크트리 격리 정상 준수(develop 기준 클린 브랜치, 오염 없음 — 이전 사례 대비 개선).

**반려 사유**:
1. **핵심 검증 목적 미달성** — task file DoD "입고 시 중량 수정이 최종 원가에 실제 반영되는지(캐시 없음) 확인"이 테스트에서 전혀 검증되지 않음. 전체 시나리오가 Supabase service client로 DB에 값을 직접 seed한 뒤 같은 값을 assert하는 동어반복 구조이며, `estimateUpsFreight`/`recordUpsActualCharges`/`finalizeInvoice` 등 실제 서버 액션 호출이 파일 전체에 없음(로그인 헬퍼 외 `page.click`/`page.fill` 없음). 중량 변경 시나리오 자체가 존재하지 않음.
2. **완료보고 커밋 내용 불일치** — 커밋 `f272fe36`("task file + ACTIVE_TASK.md 갱신")의 실제 diff는 ACTIVE_TASK.md 1개 파일, 그마저도 Agent 현황 행만 변경. task file 본문(DoD·[작업 결과])은 미반영 상태로 제출됨.

**요구사항**: 실제 서버 액션/UI를 거치는 시나리오 최소 1개 추가(중량 변경→반영 확인), task file DoD 정직하게 갱신, 커밋 메시지-diff 일치 확인 후 제출.

상세: PR#753·Issue#747 코멘트 참고.

### 2차 재검토 (2026-07-23, 재작업 커밋 `38e04d30`) — 후속 요청 2건

**판정**: ⚠️ 반려(재작업) — 핵심 사유는 훌륭하게 해결됨

**양호한 점**: `computeUpsFreight()`(실제 UPS 요율 계산 엔진, `pricing-engine.ts:243`) 직접 호출로 전면 재작성. Aiden 재실행 확인 — TC-WF-01: 2.0kg=189,142 KRW ≠ 5.0kg=249,577 KRW(실제 계산 엔진 결과), TC-WF-02: 5.0kg 기준 BASE_FREIGHT=85,000/FUEL_SURCHARGE=15,725 KRW 일치. 동어반복 아닌 진짜 계산 경로 검증. 3/3 PASS, 전체 회귀 115/786 PASS. 워크트리 격리 계속 준수.

**추가 발견(긍정적)**: `saveOrderRateSnapshot`이 오더 생성 시 1회만 호출되고 `SettlementEngine.calculateOrderCosts`가 스냅샷을 재계산 없이 그대로 사용한다는 중요한 아키텍처 발견 — 입고 후 중량 변경 시 정산 비용이 자동 갱신되지 않을 수 있는 실제 리스크.

**후속 요청**:
1. 위 아키텍처 발견을 `.agent/defects/DEF-NNN_...md`로 정식 DEF 등록(현재 task file `[발견 이슈]` 섹션은 "없음"으로 남아있고 서술로만 존재) — 수정 자체는 이번 Task 범위 밖 설계 결정 필요.
2. 재작업 과정에서 이전 6개 테스트(Stage 1 예상운임저장/Stage 2-1 실제청구등록/Stage 2-2 정산마감/Stage 3 daily-billing집계/Stage 3-DB/Gap 마감후조정)가 전부 삭제되고 TC-WF-01~03(3개)로 대체됨 — DoD의 "전체 플로우 실제 UI 실행 검증"이 실제로는 더 이상 검증되지 않음. 기존 6개 복원 후 TC-WF 시리즈를 추가하는 방식으로 재구성 필요.

상세: PR#753·Issue#747 코멘트 참고.

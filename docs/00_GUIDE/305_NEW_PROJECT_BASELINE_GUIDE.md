---
tags: ["guide", "baseline"]
---

# [305] 신규 프로젝트 BaseLine 가이드 — ZENITH_LMS 재사용 자산 이식 안내

> **문서 ID**: 305
> **분류**: 운영 & 관리(300-399) — 이식 가이드 문서
> **성격**: 🔵 **가이드 문서 — 실제 이식 작업은 수행하지 않음.** 신규(별도) 저장소를 새로 시작할 때 "무엇을 그대로 가져가고, 무엇을 어떻게 조정해야 하는지"를 판단하기 위한 참조 자료다. 여기 적힌 어떤 항목도 ZENITH_LMS 저장소 자체에는 아무 영향을 주지 않는다.
> **기준**: 2026-07-31 시점 GOV_COMMON.md(v2.8) 등 **실제 유효한(스테일하지 않은) 최신 절차** 기준으로 작성. 폐기되었거나 상위 버전으로 대체된 규칙 문구는 인용하지 않았다.
> **대상**: 새 저장소를 다수의 AI 에이전트로 착수하려는 팀 리더·오케스트레이터 에이전트
> **작성일**: 2026-07-31
> **작성자**: Aiden (Claude, ZEN_CEO)
> **버전**: v1.17 (306 §8 — v1.11~v1.16 개정 검토 의견 반영 포인터)

---

[← 목록으로 돌아가기](./000_README.md)

---

> 🔵 **별도 검토 의견 (2026-08-04, B_Kai)**: 본 가이드의 보완·추가 검토 사항은 별도 문서 [306_NEW_PROJECT_REUSE_REVIEW.md](./306_NEW_PROJECT_REUSE_REVIEW.md)로 작성되었습니다.
> **개요**: 305가 상대적으로 약한 두 축 — ①**이식 후 운영·검증 방식** ②**신규 도메인 테스트 자산의 시작점** 을 다룹니다. 구성: §2 테스트/검증 자산(A-1~A-5), §3 관리 자동화 모듈(B-1~B-3), §4 규모·환경별 조정 규칙(C), §5 결정 필요 오픈 이슈(D), §6 **본 문서 §7(v1.5) 권장안에 대한 검토 이견**(6-1~6-7), §7 **GitNexus(R-20) 활용성 평가**(7-1~7-5), §8 **v1.11~v1.16 개정 검토 의견**(K-1~K-9). B·D 항목은 본 문서 §7(v1.5)과 정합되어 있으며, RBAC 이식(§2)에 "역할 한정 화면 실브라우저 검증" 요건을 1:1로 결합하는 A-2가 최우선 보완 항목입니다. §7.2 "Issue 번호 = Task ID + ACTIVE_TASK 자동생성" 권장안에 대해서는 306 §6에서 **미검증 제안**(305 §1.6 채택 원칙 위반)으로 이견을 제시했습니다. R-20(GitNexus)은 306 §7에서 **"영향도 분석 전용" 범위 축소 이식**을 권고합니다. v1.11~v1.16 개정(§1.5-a·§1.7·§7.7·§8)에 대해서는 306 §8에서 — §7.7이 체크리스트 미연결·자동완료 구현 경로 미정의·진실 원천 축 충돌의 구조 갭 3건(K-1~K-3)과 `(N MD)` 파싱 엣지케이스(K-4)를 갖는다는 검토 의견을 제시했습니다.

## 0. 이 문서의 성격과 범위

Edward 지시(2026-07-31)에 따라 작성한다: 실제 파일 추출/새 저장소 생성 작업은 하지 않고, **"새 저장소" 기준으로 재활용 항목·수정 조정 사항을 문서로만** 남긴다. 범위는 세 가지 — **① 거버넌스/방법론 ② RBAC/권한관리 코드 스캐폴드 ③ 디자인 시스템(Zen UI)**.

각 항목은 "그대로 이식" / "조정 필요" / "이식 불가(ZENITH 전용)" 세 등급으로 판정한다.

**§1.6부터의 채택 원칙(Edward 지시, 2026-07-31)**: 이식 후보는 "이론적으로 좋아 보이는가"가 아니라 **"이 프로젝트에서 실제로 적용해 본 결과가 의미 있었는가"**로 검증한다. 실제 사고를 막았거나 실제로 반복 사용된 증거가 있는 항목만 채택하고, 문서로만 존재하거나 아직 채택되지 않은 제안, 실제로 시도했다가 폐기된 것은 "반증 사례"로만 남긴다.

---

## 1. 거버넌스/방법론 이식 가이드

### 1.1 그대로 이식 가능한 방법론 문서

아래 문서들은 물류 도메인 용어가 사실상 없거나 부수적이라, **그대로 복사해 사용 가능**하다.

| 문서 | 내용 |
|:---|:---|
| `101_ZEN_A4_METHODOLOGY.md` | 불변성 우선·함수/파일 길이 제한 등 코딩 원칙 |
| `102_INTEGRATED_DEVELOPMENT_METHODOLOGY.md` | 통합 개발 절차 |
| `103_AGENT_ROLES_SPEC.md` | 에이전트 역할 명세 템플릿(역할 이름만 신규 프로젝트에 맞게 교체) |
| `105_MULTITEAM_GOVERNANCE.md` | 다중팀 브랜치·채번 규칙(팀 이름만 교체) |
| `106_QUALITY_GATEWAY_GUIDE.md` | 품질 게이트 절차 |
| `302_OO_PERMISSION_ROUTING_GUIDE.md` | 조직타입=라우팅 축 / 역할=기능권한 축 분리 설계 원칙 |
| `303_API_FIRST_DEVELOPMENT_GUIDE.md` | 요구사항→API명세→승인→TDD→구현 순서 |
| `210_AI_AGENT_REWORK_PREVENTION_GUIDE.md` | Part B(§4~§6)가 이미 "타 프로젝트 이식용"으로 작성됨 — 그대로 사용 |
| `304_RBAC_CUSTOMFIELD_WORKFLOW_ENGINE_REVIEW.md` | §6이 이미 "신규 프로젝트 착수 시 적용 가이드"로 작성됨 — 그대로 사용 |

**주의**: 210·304는 Part A(ZENITH 근거데이터)/§1~§3 같은 실증 섹션이 함께 있다 — 이식 시 **Part B/§6 이후만** 가져가고, ZENITH 고유 사건번호(DEF/SAR 번호 등)가 담긴 실증 섹션은 "이런 방식으로 검증된 원칙"이라는 각주로만 남기는 것을 권장한다.

### 1.2 GOV_COMMON.md — R-규정 이식표

아래는 2026-07-31 기준 **실제 유효한 R-00~R-20** 전체를 조사해 판정한 것이다(폐기·상위버전 대체 내용 제외).

| 규칙 | 판정 | 조정 사항 |
|:---|:---:|:---|
| R-00 (인사/인정 입력 분석도구 호출 금지) | 원칙만 이식 | "소프트 규칙은 무력화된다 → 하드 메커니즘(Hook 레벨)으로 강제"라는 **원칙**은 이식 가치가 높다. 구체 구현(의도 분류기)은 신규 프로젝트의 실제 Hook 인프라에 맞게 재구현 필요 |
| R-01 (수행/검증 주체 명시) | 그대로 | — |
| R-03 (진척상태 최신화) | 그대로 | `WBS`/`ROADMAP` 파일이 신규 프로젝트에도 실제로 존재하는지 우선 확인 |
| R-04 (LIVE_ 체크리스트 기반 검증) | 그대로 | GUIDE/LIVE 이원화 자체가 §1.4의 핵심 패턴(아래 참조) |
| R-05 (문서 이원화: GUIDE vs LIVE) | 그대로 | 매우 유용한 패턴 — 아래 §1.4에서 별도 강조 |
| R-06 (Context7 MCP 우선) | 그대로 | 도구명 유지, MCP 서버 실제 연결 여부만 사전 확인(§1.5 참조) |
| R-07 (한글 작성 표준) | 조정 | 팀 실제 사용 언어에 맞게 교체(원칙: "핵심 문서는 팀 공용어로 통일") |
| R-08(+R-08-1/-2) (회귀테스트 의무 + CI신뢰 + DB최신화) | 원칙 그대로, 사례는 각주화 | 핵심 원칙("자가보고 회귀수치는 증거로 불인정, CI 실행결과만 인정")은 그대로. R-08-1/08-2가 인용하는 DEF-096/097·DEF-128 같은 특정 사건번호는 "실제 이런 사고를 겪은 뒤 도입된 규칙"이라는 각주로만 남긴다 |
| R-09 (회귀 테스트 확장 의무) | 그대로 | — |
| R-10 (기능-UI 결합 검증 의무) | 그대로 | 백엔드 완료≠기능 완료, 실 UI 구동 증적 요구 — 이식 가치 매우 높음(§1.2 항목1의 근거 패턴) |
| R-11/R-12 (API 설계 우선 / 명세-코드 동기화) | 그대로 | — |
| R-13 (테스트 결과물 관리 경로) | 그대로 | 폴더 경로 컨벤션만 유지 |
| R-14 (E2E 로컬 환경 표준) | 원칙만 이식 | "로컬 우선, 원격 접속은 승인 필요"라는 원칙은 유지, `Supabase` 등 구체 도구명은 신규 프로젝트의 실제 DB/BaaS로 교체 |
| R-15 (개선사항 기록, IMP) | 그대로 | 백로그 관리 패턴 자체가 유용 |
| R-16/R-17 (ACTIVE_TASK 관리체계) | 구조는 그대로, 세부는 조정 | 상태머신(⬜→📝→🔍→🔄→🔔→✅)·커밋 순서 강제·워크트리 격리·3진아웃 페널티 **구조 자체는 매우 이식 가치 높음**. `.agent/` 경로 컨벤션과 팀 로스터(Team A/B 인물명)는 신규 프로젝트 구성으로 교체. **아래 §1.3에 별도로 다룬 중요한 유의사항 있음** |
| R-18 (발견 결함 보고, DEF) | 그대로 | 채번 스크립트(`next-def-number.sh`)도 그대로 이식 가능 |
| R-19 (다중팀 거버넌스) | 조건부 | **2개 이상 팀 병행 운영 시에만 적용** — 단일팀이면 이식 불필요. 로스터는 신규 구성으로 교체 |
| R-20 (GitNexus 영향도분석 우선) | 그대로, 단 사전확인 필수 | 아래 §1.5 별도 참조 |

### 1.3 R-17 "반복 위반 페널티" — 실제로 지켜야 규정이 된다

R-17에는 "동일 유형 위반 3회 누적 시 신규 Task 할당 일시 중단 + 재교육"이라는 명문 조항이 있다. 이 조항 **자체**는 이식 가치가 매우 높지만, **실제로 집행하지 않으면 문서로만 존재하는 죽은 규정이 된다**는 것이 ZENITH_LMS에서 실증된 교훈이다(GitHub Issue #987 — 여러 인원이 3회 기준을 여러 유형에서 반복 초과했음에도 실제 집행이 이뤄지지 않아, 같은 유형의 위반이 계속 재발했다). 신규 프로젝트에 이 조항을 이식할 때는:

- 3회 기준 도달 시 **예외 없이 실제 집행**하거나, 애초에 다른 기준(예: 심각도별 즉시 조치)으로 대체할 것 — "일단 만들어두고 실제로는 안 지킨다"가 가장 나쁜 선택지다.
- "위반 횟수를 소급 면제/미가산 처리"하는 예외를 **여러 번 반복 적용하지 않는다** — 이 자체가 카운터를 무력화하는 구조적 허점이 된다.

**기록만으로는 부족 — 자동 게이트와 연결할 것(306 §B-3)**: `.agent/VIOLATION_TRACKER.md`는 위반 이력 가시화에는 효과가 있었지만(Issue #987 분석 근거), 기록 자체가 재발을 막지는 못했다. `pr-checks.yml`의 `task-file-check` job도 advisory(비차단) 경고에 그쳐 실효가 없었다. 신규 프로젝트는 착수부터 기록과 **자동 게이트**를 함께 설계할 것:
1. 위반 카운터 N회 초과 시 **PR 생성 자체를 차단**(CI required check)
2. `task-file-check` 류 검증을 언제·어떤 근거로 비차단→차단 게이트로 승격할지 기준을 사전에 명문화
3. `insert-commit-hash.sh` 같은 수기 오류 방지 스크립트는 그대로 이식

### 1.4 구조적 패턴 — GUIDE/LIVE 이원화, 공통규칙 인라인 로드

두 가지 구조 패턴은 코드가 아니라 **문서 조직 방식** 자체가 재사용 자산이다:

1. **GUIDE(불변 표준) vs LIVE(현재 진행 상태) 분리**(R-05) — "표준은 잘 안 바뀌고, 진행상태는 매일 바뀐다"는 것을 문서 구조로 분리해 AI 에이전트가 매번 전체를 다시 읽지 않아도 되게 한다.
2. **공통 규칙(GOV_COMMON.md) + 에이전트별 파일(CLAUDE.md/AGENTS.md/GEMINI.md)의 인라인 로드 구조** — `@GOV_COMMON.md` 형태로 공통 규칙을 한 곳에 두고, 각 에이전트 전용 파일(세션 초기화 절차, 커밋 태그 등 도구별로 다른 내용)은 별도 유지한다. 이 분리 덕분에 R-01~R-20 같은 공통 규칙을 한 번만 고치면 모든 에이전트 도구에 자동 반영된다.

### 1.5 신규 채택 전 반드시 확인할 것 — 도구 실제 연결 여부

R-06(Context7 MCP)·R-20(GitNexus MCP) 둘 다 "이 MCP 도구를 우선 사용하라"는 규칙인데, ZENITH_LMS에서 실제로 다음이 확인됐다: **문서에 규칙을 적어놓아도, 각 에이전트의 실행 환경(모델/CLI)에 해당 MCP 서버가 실제로 연결되어 있는지는 별개 문제**다. Team B(비-Claude Code 모델 사용)에 GitNexus MCP가 실제로 연결되어 있는지 확인이 안 된 채로 R-20을 적용해버린 사례가 있다(GitHub Issue #987 코멘트 참조). 신규 프로젝트에서 MCP 기반 규칙을 채택할 때는, **모든 참여 에이전트의 실행 환경에서 실제로 해당 MCP 도구가 호출 가능한지 사전에 확인**한 뒤 규정화할 것.

### 1.5-a GitNexus(R-20) 활용성 평가 — "영향도 분석 전용"으로 범위 축소 이식, 대안 도구 포함

Edward 요청(2026-08-04)에 따라 실측 평가한다. 306(B_Kai) §7·308(D_Kai) §4가 서로 다른 모델로 독립 실사했음에도 **동일한 결론에 수렴**했다 — 교차검증된 평가로 신뢰도가 높다.

**실증된 가치**: DEF-B-031/032/033 분석에서 grep/직접 읽기로 놓친 `canChangeStatus()`(역할×상태 중앙 게이트) 존재와 UPS 전용/일반 물류 가격엔진 분리 사실을 GitNexus 사용 즉시 발견(GOV_COMMON.md R-20 근거) — **교차 심볼 관계 탐색·편집 전 영향도 분석(impact)·A→B 경로 추적(trace)**은 grep으로 여러 홉이 필요한 걸 1회 호출로 해결하는 실질 우위가 있다.

**제약(이식 시 반드시 함께 전달할 것)**:
- **인덱스 staleness가 최대 실패 모드**: R-20 신설 근거 자체가 인덱스가 HEAD 대비 **1813커밋** 뒤처진 상태에서의 분석을 맹신했던 사고였다(2026-08-04 실측 시점은 1커밋 지연으로 양호하나, 관리를 놓으면 언제든 재발 가능). "사용 강제"만으로는 부족하고 **"인덱스 freshness 확인 → 결과 재검증" 절차를 규칙 텍스트에 함께 명문화**해야 한다.
- **개념 탐색(`query`)은 한정적**: 실측 결과 `embeddings: 0`·`vectorSearch: unavailable`·`cjkSegmentation: none` — 한국어 시맨틱 검색이 안 되고 BM25 텍스트 매칭만 동작한다. 한국어 주석·문서가 많은 리포에서는 `query` 탐색을 grep과 병행할 것. 반대로 식별자가 영어 위주인 TypeScript 리포일수록 효용이 크다.
- **과잉 분석 위험(검증 완료, 단 이미 조치된 역사적 사례)**: SAR-2026-05-13-001은 에이전트가 "전면 분석을 시도"한 게 아니라, **Grep/Glob/Bash 호출 시마다 GitNexus Hook이 6,827개 심볼을 자동으로 컨텍스트에 주입**하는 구조("GitNexus Hook Storm", 근본원인 20% 비중)가 특정 모델(GLM 계열, 대컨텍스트 편향)의 "많이 읽었으니 전면 분석 필요" 판단을 유발해 반복 재작업으로 이어진 사례였다. **이 문제 자체는 이미 조치 완료**(GOV-002 Hook 중복 제거, GOV-004 PreToolUse Bash 제외 — 현재 CLAUDE.md에도 "Bash가 자동 주입 대상에서 제외됨"으로 반영돼 있다). 신규 프로젝트에는 결과가 아니라 **원인 구조**를 이식할 것 — Hook을 다수 도구 호출(Grep/Glob/Bash 등)에 무분별하게 걸지 말고, "수정 전 좁은 범위 `impact` 호출 시에만" 컨텍스트를 주입하도록 처음부터 설계할 것.
- **환경 연결은 별개 문제**(위 본문과 동일 원칙), **taint/PDG 분석(`explain`)은 `--pdg` 인덱스 별도 구축 필요**하며 클로저·콜백·프로퍼티 흐름은 공식적으로 미추적 — 참고용으로만 취급.
- **운영 비용**: 인덱스 DB 178MB + 야간 재인덱싱 cron 운영이 필요 — 심볼 수가 적은 초기 프로젝트나 Tier 1(단일 개발자)에는 오버헤드가 이득보다 클 수 있다.

**staleness 완화 — post-commit hook 재인덱싱 (307 §4-3, 검증 후 반영)**: 야간 cron(수시간 지연) 대신 `.git/hooks/post-commit`에서 커밋마다 백그라운드로 재인덱싱하는 방안이 307에서 제안됐다. 실측 근거(재인덱싱 소요시간 약 24.6초, 10,296 노드 기준)로 볼 때 **커밋마다 백그라운드 실행은 실제로 타당**하다. 다만 이식 시 반드시 정정할 것 — ① 원안의 `npx gitnexus analyze` 명령은 `rtk` 같은 CLI 재작성 훅이 있는 환경에서 `npx <pkg>`를 `npm run <pkg>`로 오변환해 실패하는 게 실측 확인됐다(`Missing script: "gitnexus"`) — **`npx` 없이 `gitnexus analyze`를 직접 호출**할 것. ② 물리적 인덱스 파일이 갱신되어도 **같은 세션 안에서는 GitNexus MCP 리소스가 세션 시작 시점의 캐시를 계속 반환**한다 — post-commit hook만으로는 부족하고, 재인덱싱 성공 여부는 리소스 문구가 아니라 인덱스 메타파일의 `lastCommit`을 직접 비교해 판단하며, 진짜 최신 결과를 에이전트가 보려면 세션(MCP 서버) 재시작이 필요함을 문서화할 것.

**MCP 미연결 환경 대응 — CLI 폴백 + 명시적 대체 규칙 (307 §4-3, 방향만 채택)**: §1.5의 "사전 확인" 원칙에서 한 걸음 더 나아가, MCP가 연결되지 않는 에이전트 실행환경을 위해 **CLI 직접 호출 폴백 + `tsc --noEmit`/grep 기반 대체 절차를 R-20 예외 조항으로 명문화**하는 방향은 타당하다. 다만 307이 언급한 특정 도구명(전용 CLI 래퍼)은 존재 여부를 확인할 수 없는 미검증 주장이므로, "이런 대체 수단이 있다"가 아니라 **"MCP 미연결 시 무엇으로 대체할지를 신규 프로젝트가 직접 정의"**하는 정도로만 이식할 것.

**미검증으로 보류(반영하지 않음)**: 307은 GitNexus가 "에이전트 탐색 토큰을 60% 이상 절감"한다고 주장하나 리포 전체에서 이를 뒷받침할 근거를 찾지 못했다 — 이전에 308이 반증한 "UI 재작업 80% 감축" 주장과 동일한 패턴(출처 없는 구체적 수치)이라 채택하지 않는다. "Next.js 동적 호출(RSC↔Client 경계) 탐지 누락"도 정적분석 도구 일반의 개연성 있는 한계이나 이 리포에서 실제 사례가 확인되지 않아 "그럴듯하나 미검증"으로만 남긴다 — 도입한다면 정적 분석 결과를 런타임 테스트(예: 관련 테스트 재실행)로 교차 검증하는 습관 정도가 안전한 보완책이다.

**대안 도구 비교** (GitNexus를 전면 대체하기보다 용도별로 나눠 볼 것):

| 용도 | 대안 | 비고 |
|:---|:---|:---|
| 순수 TS↔TS 호출관계(find references) | **TypeScript LSP**(에디터 내장 언어서버) | 인덱스 방치 문제 자체가 구조적으로 없음(항상 라이브) — 흔한 케이스엔 더 저렴하고 정확 |
| 임포트 순환/아키텍처 시각화 | `madge`, `dependency-cruiser` | GitNexus의 `check()`가 이미 이 기능을 포함하므로 GitNexus 채택 시엔 중복 |
| 패턴 기반 구조 검색 | `ast-grep` | grep보다 문법 인식이 정확, 별도 인덱스 없음 |
| **TS 코드 → SQL/RPC 함수를 잇는 크로스 언어 blast radius**(GitNexus의 실제 차별점) | 뚜렷한 대안 없음 | LSP는 TS 안에서만 동작해 DB 레이어를 못 봄 — 이 용도는 GitNexus류 그래프 도구가 아니면 대체가 어려움 |

**신규 프로젝트 이식 판정**: Tier 2 이상(멀티 에이전트, §1.7)에서만 조건부 채택. 채택 시 **"도구 자체 + 인덱스 freshness 검증 절차 + 전 참여 에이전트 환경 연결 확인"** 3종 세트로 이식할 것 — 도구만 복사하면 ZENITH_LMS가 겪은 두 실패(1813커밋 staleness, 환경 미연결)를 그대로 반복한다. GitNexus 미사용/미검증을 VIOLATION_TRACKER류 위반 유형으로 편입해, R-17의 "기록만으로는 부족, 게이트와 연결" 원칙(§1.3)을 동일하게 적용할 것.

### 1.6 VIOLATION_TRACKER.md — 가시화엔 실증, 재발방지엔 반증

문서/폴더 관리(206)·Multi-Agent 운영(104/107) 계열 후보를 §1.6 원칙(실제 적용 결과 기준)으로 재검토한 결과, 대부분 채택 기준을 충족하지 못했다. 유일하게 **조건부로 실증된 항목**이 VIOLATION_TRACKER.md다:

- **실증된 부분**: 위반 이력을 태스크와 분리해 단일 파일에 누적 기록하는 방식이, 이번 세션에서 Team B의 반복 위반 패턴(동일 유형·동일 인물)을 실제로 드러내는 데 효과가 있었다(GitHub Issue #987 분석의 근거 자체가 이 파일이었다) — **가시화 도구로는 채택**.
- **반증된 부분**: 그러나 꼼꼼히 기록되는 것과 재발이 실제로 줄어드는 것은 별개였다 — 같은 파일에 계속 기록되면서도 같은 사람의 같은 유형 위반이 반복됐다(§1.3의 R-17 미집행 사례와 동일 근거). **"기록하면 저절로 개선된다"는 기대는 채택 금지** — 반드시 §1.3처럼 실제 조치(할당중단 등)와 짝지어야 의미가 있다.

**검토했으나 채택하지 않은 항목** (투명성을 위해 근거와 함께 남긴다 — 향후 같은 후보를 다시 검토하는 시간 낭비를 막기 위함):

| 후보 | 미채택 사유 |
|:---|:---|
| `206_DOCS_AND_SCRIPT_GOVERNANCE.md`의 "문서번호 수동 확인" 컨벤션 | 소프트 규칙이라 실제로는 계속 드리프트(`04_Database` vs `database`, `09_Checklists` vs `09_TEMPLATES` 중복 폴더가 지금도 존재) — 실제 효과가 있었던 건 이 수동 컨벤션이 아니라 자동화 스크립트(`next-task-number.sh` 등, 이미 §1.2에 반영)였다 |
| Obsidian Vault 제안(206 내) | 문서 자체가 "에이전트 워크플로우엔 영향 없음"이라 명시 — 채택·실사용 증거 전무 |
| `107_MULTIAGENT_SCALING_PROPOSAL.md` | 문서 상태가 "제안 단계, 검토 대기"로 **아직 채택 안 됨** — 실제 적용 결과가 없다. 문제 진단(채번 경쟁조건 등)은 실제 사고와 일치해 근거가 있으나, 제안된 해법 자체는 미검증 |
| `104_MULTIAGENT_RNR_GUIDE.md` | 103_AGENT_ROLES_SPEC.md의 초안으로 추정되고 실제로는 103만 참조됨 — 독자적 실증 증거 없음, 103과 중복 |
| SQL/스크립트 관리 규칙(206 내, migrations CLI 전용 등) | 합리적이나, 이 규칙 덕분에 실제 사고를 막았다는 구체 사례를 확인하지 못함 — 아직 "검증된 규칙"은 아님 |

### 1.7 규모별 채택 폭 조정 — 전체 거버넌스가 항상 정답은 아니다(306 §C-1)

이 문서(§1~§8) 전체가 **ZENITH_LMS(다중 에이전트 + 다중 팀 초대형) 전제**로 작성되었다. 소규모(1~2인·단일팀) 신규 프로젝트에 R-17 상태머신 + 워크트리 격리 + sync 워크플로우 3종을 전부 이식하면 **관리 비용이 개발 비용을 초과**할 수 있다. 착수 전 아래 Tier로 스코프를 먼저 결정할 것:

| Tier | 기준 | 최소 채택 세트 |
|:---|:---|:---|
| **Tier 1** | 1~2인 · 단일 에이전트 | GOV_COMMON 축소판 · R-08 회귀 · Issue 템플릿 · CI |
| **Tier 2** | 소수 인원 · 멀티 에이전트 | + ACTIVE_TASK 상태머신 · 채번 스크립트(또는 §7.2 파일럿안) · 워크트리 격리 |
| **Tier 3** | 다중 팀 병행(≥2팀) | 본 문서 전면 적용(R-19 포함) · sync 워크플로우 3종 · 라벨 스키마 |

§7.2의 "Issue=Task ID" 파일럿안은 Tier 2 이상에서만 참고안으로 검토한다(§7.2 참조). GitNexus(§1.5-a) 채택 여부도 동일하게 이 Tier 게이지에 연동할 것.

**착수 로드맵 0단계로 이 Tier 확정을 가장 먼저 둘 것(308 §2-1)**: 로드맵 후속 단계(거버넌스 셋업·코드 스캐폴드·프로세스 표준화, §8 체크리스트 참조)를 "전면 적용" 전제로 계획하면 소규모 프로젝트에서 과잉 투입이 반복된다 — Tier를 먼저 정하고, 그에 맞는 최소 채택 세트만 이후 단계에 반영할 것.

---

## 2. RBAC/권한관리 코드 스캐폴드 이식 가이드

### 2.1 재사용할 패턴 (구조 자체는 우수)

`src/lib/auth/rbac.ts` + `guards.ts` + `zen_role_permissions` 테이블 조합은 다음 패턴을 구현한다 — **이 패턴 자체는 이식 가치가 높다**:

```
역할(Role) × 경로/리소스(Path) → 허용 여부
  ├── STATIC_PERMISSIONS: 코드 내 fallback 맵(DB 장애 시 대비)
  ├── zen_role_permissions 테이블: DB 기반 동적 권한(관리자 UI로 CRUD 가능)
  └── checkPermission()/checkPermissionDB(): 서버 액션 진입점에서 호출하는 단일 진입점 함수
```

- `normalizePath()`로 다국어 라우트 접두사(`/ko`, `/en` 등)를 제거하고 순수 경로로 비교하는 방식 — 다국어 라우팅을 쓰는 프로젝트라면 그대로 유용
- 미들웨어(`proxy.ts`) 레벨 화이트리스트와 앱 레벨(`rbac.ts`) 권한표가 **별도 관리**된다는 점 — 두 레벨이 있다는 구조 자체는 정상이나, 아래 §2.2에서 다루는 위험을 안고 있다

### 2.2 반드시 리네이밍/재정의해야 하는 것

- **`USER_ROLES` 13개 열거값**(`ZENITH_SUPER_ADMIN`/`CARRIER`/`CUSTOMS_BROKER`/`AGENCY_SHIPPER` 등) — 전부 ZENITH 물류 도메인 특화. 신규 프로젝트의 실제 역할로 전면 교체 필요
- **`STATIC_PERMISSIONS`의 하드코딩된 경로 목록**(`/admin/ups-rates`, `/logistics`, `/warehouse` 등) — 신규 프로젝트 라우트 구조로 전면 교체
- **`zen_` 테이블 접두사** — 신규 프로젝트 네이밍 컨벤션에 맞게 교체(접두사 자체를 유지할지는 팀 선택)

### 2.3 알려진 구조적 위험 — 반드시 함께 이식할 것

ZENITH_LMS에서 **AGENCY 역할 하나를 여러 권한 체크 지점에 빠뜨리는 사고가 8회 이상 반복**됐다(DEF-114/116/117/DEF-B-002/010/014/017/019). 근본 원인은 "미들웨어 화이트리스트·앱 레벨 권한표·RLS 정책·DB GRANT가 각각 독립적으로 관리되어, 신규 역할 추가 시 네 곳 중 하나를 빠뜨리기 쉬운 구조"다. 이 RBAC 스캐폴드를 이식할 때는 반드시 **304 문서(§4.3)가 제안하는 "역할×리소스×액션을 하나의 선언적 소스에서 코드생성"하는 방향을 함께 검토**할 것 — 스캐폴드만 복사하고 이 교훈을 빠뜨리면 같은 사고가 신규 프로젝트에서도 반복될 가능성이 높다.

### 2.4 검증 요건과의 결합 — "역할 한정 화면은 실브라우저 검증 필수"(306 §A-2)

§2.1~§2.3은 **이식 대상**만 다룬다. RBAC를 이식하면서 **이에 대응하는 검증 규정을 함께 이식하지 않으면** 같은 사고가 신규 프로젝트에서 재발한다. 근거: 역할·조직유형 한정 화면은 단위 테스트(서버 액션 직접 호출, `validateUserAction` mock)만으로는 미들웨어(`proxy.ts`)의 라우팅 화이트리스트 버그를 잡지 못한다 — 실제 사례로 Issue #688(SUB_ADMIN 역할 라우팅 버그가 단위 테스트로 미검출)이 있다. `rbac.ts`의 `STATIC_PERMISSIONS`(앱 레벨)와 `proxy.ts` 화이트리스트(미들웨어 레벨)는 별도 관리되므로, 실제 브라우저 네비게이션으로 둘의 일치를 확인해야만 잡을 수 있다.

**이식 시 함께 명문화할 것**:
- RBAC 스캐폴드 이식 시, 역할·조직유형 한정 화면은 **실제 로그인 → `page.goto()` 네비게이션**으로 검증 의무화(R-10과 결합)
- 미들웨어 화이트리스트 ↔ 앱 레벨 권한표 ↔ RLS ↔ DB GRANT의 **4계층 일치**를 확인하는 전용 E2E 케이스를 착수 스캐폴드에 포함

---

## 3. 디자인 시스템(Zen UI) 이식 가이드

### 3.1 컴포넌트 — 그대로 이식 가능

`src/components/ui/`의 `ZenButton`·`ZenCard`·`ZenInput`·`ZenSelect`·`ZenTextarea`·`ZenBadge`·`ZenDataGrid`·`ZenErrorView`·`ZenAurora`·`ZenUI` — 비즈니스 로직과 결합되어 있지 않은 순수 프레젠테이션 컴포넌트다. 그대로 복사해 사용 가능하며, `Zen` 접두사만 신규 프로젝트의 네이밍 컨벤션으로 바꾸면 된다.

### 3.2 디자인 토큰 — 분리 작업 필요

`src/app/globals.css`에 CSS 커스텀 프로퍼티로 토큰이 정의되어 있으나(`--color-brand-*`, `--zen-shadow-*`, `--zen-glass-*` 등), **순수 디자인 토큰(색상·그림자·트랜지션)과 ZENITH 레이아웃 상수(`--sidebar-width: 280px`, `--header-height: 64px` 등)가 한 파일에 섞여 있다.** 예: `--color-accent: #f59e0b; /* Logistics Amber */` — 값 자체는 범용이나 주석이 도메인 특화다.

이식 시:
1. 순수 토큰(색상 팔레트·그림자·트랜지션·폰트 변수)만 별도 `tokens.css` 등으로 분리 추출
2. 레이아웃 상수(사이드바 너비 등)는 신규 프로젝트 화면 구조에 맞게 재정의
3. Tailwind v4 CSS-first 방식(`tailwind.config.*` 파일 없이 globals.css 안에서 `@theme` 등으로 정의)을 그대로 따를지, 별도 config 파일 방식으로 바꿀지는 팀 선택

---

## 4. 다국어·통화·테마·커스텀필드·워크플로우 이식 가이드

304 문서(Redmine 비교분석) 검토 시 다루지 않았던 나머지 기술 영역을 코드 실사 기준으로 점검한 결과다. 이 다섯 영역은 성숙도 편차가 크다 — **그대로 이식 가능한 것은 하나도 없고, 전부 "패턴은 참고, 구현은 보강 필요"** 등급이다.

### 4.1 다국어(i18n) — 라우팅 골격은 이식 가능, 네임스페이스 충돌 위험 있음

- **구성**: `next-intl`, `src/i18n/routing.ts`(ko/en/zh/ja, 기본 ko) + `src/i18n/request.ts` + `src/app/[locale]/...` 라우트 구조. `messages/{locale}.json`이 기능별 네임스페이스(`Common`/`Navigation`/`Orders`/`UpsInvoice` 등 45개)로 중첩 구성 — flat 구조 아님
- **결함**: 번역키 관련 결함 최소 3건(DEF-115, DEF-061, DEF-B-023). 특히 DEF-B-023은 **네임스페이스를 다른 화면이 재사용하다 28개 키가 충돌**해 페이지당 25건 이상 `MISSING_MESSAGE` 오류가 난 사례 — 네임스페이스 소유권 규칙 부재가 근본원인
- **갭**: 로케일 전환 UI 컴포넌트 자체가 없음 — 라우팅/미들웨어 레벨엔 4개 로케일이 연결돼 있는데 실제 사용자가 언어를 바꿀 방법이 UI에 없음
- **판정**: 라우팅/설정 골격은 그대로 이식. 다만 **네임스페이스 소유권 컨벤션**(어느 화면이 어느 네임스페이스를 쓰는지 명문화)과 **로케일 전환 UI**를 함께 추가해야 재발을 막는다

### 4.2 통화(currency) 처리 — 표시 컴포넌트만 재사용, 변환 로직은 비추천

- **구성**: 공통 변환 유틸리티 없음 — 표시는 `ZenCurrencyDisplay.tsx`(`Intl.NumberFormat` 기반, currency/code-prefix/code-suffix 3종 변형)로 처리하지만, 실제 환율(`applied_exchange_rate`) 값과 변환 로직은 `invoice.ts`/`daily-billing.ts`/`settlement.ts`/`invoice-generator.ts`에 **각각 인라인으로 흩어져 있음**(중앙화 안 됨)
- **결함**: 통화/요율 관련 결함 최소 7건(DEF-B-021 KRW/USD 오표기, DEF-B-032, DEF-B-013/014, DEF-067, DEF-088 등) — 이번 세션에서 다룬 DEF-B-031~034 연쇄도 넓게 보면 같은 계열
- **판정**: `ZenCurrencyDisplay` 표시 컴포넌트만 그대로 재사용. **변환/환율 로직은 이식 비추천** — 중앙화된 단일 함수 없이 여러 곳에 흩어진 구조 자체가 이 세션에서 확인한 결함들의 근본 원인과 같은 패턴(§4.1 GOV_COMMON.md 이식표에서 다룬 "단일 소스" 원칙 위반)이다

### 4.3 화면 Skin/테마 — 이식 비추천, 처음부터 다시 설계할 것

- **구성**: `next-themes` 패키지가 `package.json`에 있으나 **`ThemeProvider`/`useTheme` 사용처가 코드 전체에 단 한 곳도 없음**(설치만 되고 미사용). 다크모드는 Tailwind `dark:` 유틸리티 클래스를 컴포넌트마다 수동으로 흩뿌리는 방식이며, `data-theme` 토글이나 `localStorage` 영속화 없이 OS `prefers-color-scheme`에만 반응
- **결함**: DEF-B-030(상세 테이블이 요약 테이블 마크업을 복사하면서 `dark:` 변형을 통째로 누락)이 이 취약성을 정확히 보여줌 — 테마 적용이 컴포넌트별 수기 작업이라 강제 메커니즘이 없음
- **판정**: **이식 비추천**. 설치돼 있는 `next-themes`를 실제로 연결(ThemeProvider + 토글 + 영속화)하고, 시맨틱 컬러 토큰(예: `bg-surface`, `text-default`)을 통해 다크모드가 컴포넌트 작성자의 수기 작업 없이도 자동 적용되는 구조로 새로 설계하는 게 낫다

### 4.4 커스텀필드 — 304 문서 §4.4 참조

이미 [304 문서](304_RBAC_CUSTOMFIELD_WORKFLOW_ENGINE_REVIEW.md) §4.4에서 다뤘다 — **순수 EAV(Redmine 방식)는 기각, JSONB+필드정의 테이블 하이브리드를 권장**(Postgres 기반 프로젝트 한정). ZENITH_LMS엔 현재 커스텀필드 기능 자체가 없어 이식할 기존 코드가 없고, 신규 프로젝트에서 필요해지면 304 §4.4의 설계 원칙부터 적용할 것.

### 4.5 워크플로우 엔진 — 304 문서 §4.5 참조 + DECISIONS.md #13 진행상황 확인

이미 [304 문서](304_RBAC_CUSTOMFIELD_WORKFLOW_ENGINE_REVIEW.md) §4.5에서 다뤘다 — 도메인에 **"변형 축"**(ZENITH의 `transport_mode`처럼, 같은 리소스가 축에 따라 다른 규칙을 갖는 경우)이 있는지 먼저 확인하고, 있다면 (역할×변형×상태전이) 관계형 트리플 테이블로 처음부터 설계할 것을 권장한다. ZENITH_LMS 자체는 이 설계를 아직 적용하지 않았고(`.planning/DECISIONS.md` #13, 통합 vs 분리 여부 보류 중) 기존 `canChangeStatus()`가 변형 축 없이 역할×상태만 다루는 상태다 — 신규 프로젝트라면 이 갭을 처음부터 피할 수 있다.

---

## 5. 기타 인프라/도구 이식 가이드

거버넌스·RBAC·디자인·다국어/통화/테마 외에, 코드 실사로 추가 확인한 8개 항목이다. 이 중 절반은 **도메인 결합이 전혀 없어 그대로 복사 가능**하다.

### 5.1 그대로 이식 가능 (portable as-is)

| 항목 | 위치 | 요지 |
|:---|:---|:---|
| **Seed 멱등성 패턴** | `scripts/seed-local.ts`의 `createUser()`/`getOrCreateOrg()` | "자연키(이메일/이름)로 먼저 조회 → 없을 때만 생성" — ZENITH 결합 전혀 없음, 어떤 Supabase 프로젝트에도 그대로 복사 가능 |
| **Rate Limiting** | `src/lib/security/rate-limit.ts` | 외부 의존성 없는 순수 슬라이딩 윈도우(`InMemoryRateLimiter`, Map 기반 타임스탬프 배열) — 완전히 도메인 무관 |
| **알림/이메일 발송** | `src/lib/notifications/email.ts` | Resend SDK 래퍼(`sendViaResend()`) — 키 미설정 시 조용히 no-op, 구조화 로깅. 래퍼 자체는 범용, 호출부(주문/인보이스 알림)만 비즈니스 특화 |
| **테스트 3계층 구조** | `tests/{unit,integration,e2e}/` | 구조 자체가 범용. vacuous-test 안티패턴 카탈로그(6종)는 이미 210 가이드 §2에 있음 — 그대로 참조 |
| **외부 API 호출 감사로그** | `zen_shxk_api_logs` 테이블 + `src/lib/shxk/client.ts` | "모든 외부 호출을 요청·응답·상태와 함께 기록"하는 패턴 자체는 범용 — 테이블명·필드명만 리네이밍하면 그대로 사용 가능 |

### 5.2 조정 필요

| 항목 | 위치 | 조정 사항 |
|:---|:---|:---|
| **CI 파이프라인** | `.github/workflows/pr-checks.yml` | Supabase 기동→마이그레이션→빌드→회귀테스트 골격은 재사용 가능. `task-file-check` job만 `.agent/tasks/` 컨벤션에 결합돼 있어 제거·일반화 필요 |
| **파일/문서 스토리지** | `zen_ups_label_documents` 패턴(Issue #684) | "메타데이터 테이블 1개 + storage-path에 소유권 인코딩 + signed URL 발급" 구조는 범용. 필드명(`label_id` 등)과 UPS 전용 RLS 배선은 `parent_entity_id`/`doc_type` 형태로 일반화 필요 |
| **배포 SOP** | `208_SUPABASE_REMOTE_SOP.md`(그대로 이식 가능, 이미 `[PROJECT_ID]` 플레이스홀더 사용) / `209_VERCEL_DEPLOYMENT_SOP.md`(실제 프로젝트 ref가 5곳에 하드코딩돼 있어 플레이스홀더 치환 필요) | 208은 그대로, 209만 치환 작업 필요 |

---

## 6. 저장소(리포지토리) 구조 방안 — 멀티 리포 분리 검토

Edward 요청(2026-08-03/04)에 따라 검토한다: ZENITH_LMS는 현재 단일 리포 운영이며, 이 §6은 **ZENITH_LMS 자체를 재구성하는 방안이 아니라 신규 프로젝트를 처음부터 멀티 리포로 착수할 때의 방안**이다(§0 원칙과 동일 — 실제 분리 작업은 이 문서 범위 밖).

### 6.0 배경 — ZENITH_LMS에서 실측 확인된 문제

- `docs/` 67MB 중 59MB가 바이너리(스크린샷·PDF·엑셀, 주로 `99_Manual/`의 E2E 증적)이고, 소스코드(`src/`)는 3.5MB에 불과 — 바이너리가 코드의 약 17배.
- `.git` 143MB 중 상당 부분이 이 바이너리 이력. `.gitattributes`/git-lfs가 설정되어 있지 않아, 같은 스크린샷 파일이 커밋마다 풀사이즈로 **7~8버전씩 누적**되는 것을 실측 확인(예: `docs/99_Manual/E2E_26_Result/01_label_not_issued.png`).
- 원인은 "원자료(스크린샷 등)가 분석/설계 단계에서 1회성으로 소비된 뒤 `.md`로 증류되고, 이후에는 재조회 빈도가 사실상 0에 가까운" 라이프사이클과 "git은 모든 버전을 영구 보존한다"는 전제가 서로 안 맞기 때문.

### 6.1 분리 기준 — "바이너리냐/텍스트냐"·"변경빈도"가 아니라 "코드와의 결합도"

신규 프로젝트에서 멀티 리포를 설계할 때 가장 중요한 판단은 파일 형식(바이너리 vs 텍스트)이나 변경 주기(수시 vs 주/월 단위)가 아니라, **"이 문서가 코드 커밋과 원자적으로 함께 검증돼야 하는가"**다.

- R-12(API 사양 변경 시 명세서 선제 업데이트)·R-04(LIVE_ 체크리스트가 완료판정의 근거)처럼, 코드와 **같은 PR/같은 커밋 시점**에 함께 검증돼야 하는 문서(API 명세·DB 스키마 문서·LIVE_ 체크리스트)를 코드와 다른 리포로 쪼개면 이 동시성 검증 자체가 깨진다 — 멀티 리포의 전형적 실패 패턴(원자적 커밋 불가)이 여기서 발생한다.
- 반대로 코드 커밋과 무관하게 독립적으로 생성·소비되는 자료(원자료/스크린샷/사업관리 보고서 등)는 결합도가 낮아 분리해도 이 문제가 생기지 않는다. 실제로 ZENITH_LMS에서도 결합도 낮은 자료(`99_Manual`·`80_RawData`·`98_images`)가 바이너리 대부분과 거의 정확히 겹친다 — 두 기준(결합도/저빈도)이 우연이 아니라 서로 연동되는 경향이 있다.

### 6.2 권장 구조 — 2-리포 분리 (Core / Reference)

| 리포 | 성격 | 포함 대상 |
|:---|:---|:---|
| **리포 A (Core)** | 코드와 결합도 높음, 수시 변경, 원자적 커밋 필요 | 소스코드 전체 + `00_GUIDE`(거버넌스, §1) + API 명세·DB 스키마 문서(§1.2 R-11/R-12 대상) + LIVE_ 체크리스트/SAR(R-04/R-13 대상) + `.agent/`(Task 관리, §1.2 R-16/R-17) + 방법론/설계 문서 |
| **리포 B (Reference/Archive)** | 코드와 결합도 낮음, 저빈도 참조, 원자적 커밋 불필요 | 원자료(스크린샷·PDF·엑셀 등 분석/설계 1회성 소비 자료), R-10/R-17 감사 증적(E2E 스크린샷 등 완료 후 재조회 목적), 사업관리 보고서(진척/위험/주간/월간 — 코드 커밋 시점과 무관하게 독립 생성) |

세 번째 리포(사업관리 문서 전용)는 기술적 필요보다 **접근권한 분리**(경영진 전용 vs 개발자 공개 등)가 조직적으로 필요할 때만 고려 — 기본값은 2-리포.

### 6.3 리포 B의 바이너리 관리 원칙

- git-lfs 채택 여부를 **자료 성격별로 구분**해서 결정할 것 — 일괄 적용이 능사가 아니다.
  - "1회성 소비 후 재조회 안 됨"이 명확한 원자료(분석 착수 단계의 스크린샷·PDF 등)는 애초에 **버전 이력 자체에 가치가 없다** — git-lfs보다 "최신본만 유지, 과거본은 커밋 이력에 안 남기는" 정책(별도 브랜치에서 스쿼시, 혹은 최종본만 커밋)이 더 단순하고 충분.
  - R-10/R-17처럼 **이력 자체가 감사 증적**인 파일(이 스크린샷이 이 시점에 이 상태였다는 것이 증거)은 git-lfs 적용 권장 — 포인터만 메인 이력에 남아 리포 크기를 억제하면서도 버전 추적은 유지.
- 착수 시점에 `.gitattributes`로 이미지/PDF/xlsx 확장자를 미리 lfs 트래킹 대상으로 지정해두면, ZENITH_LMS처럼 나중에 143MB가 쌓인 뒤 파괴적인 히스토리 재작성(`git filter-repo` 등, 전원 재클론 필요)을 하는 상황 자체를 예방할 수 있다.

### 6.4 상호 참조 방식 — 느슨한 링크 권장, Submodule/Subtree 비권장

| 방식 | 판정 | 사유 |
|:---|:---:|:---|
| Git Submodule | 비권장 | 정확한 커밋 고정은 가능하나 UX 마찰이 크고(detached HEAD, 포인터 갱신 누락 빈발), 저빈도 참조자료에는 관리 비용이 이득보다 큼 |
| Git Subtree | 비권장 | 참조 리포의 바이너리 이력을 메인 리포로 다시 끌어들여, 애초에 분리하려던 목적(리포 A 경량화) 자체를 무효화함 |
| **느슨한 링크 참조**(리포 A 문서에서 리포 B의 URL을 직접 하이퍼링크) | **권장** | 빌드/실행 시점에 물리적으로 필요한 게 아니라 사람·에이전트가 "가서 확인"하는 용도이므로, 별도 git 연결 기능 없이 URL 링크 + 리포 B 내부의 Task/DEF 번호 ↔ 증적파일 매핑 인덱스(매니페스트) 문서만으로 충분 |

Submodule/Subtree는 원래 "빌드 시점에 다른 리포 내용이 물리적으로 필요한 경우"(공유 라이브러리 등)를 위한 도구라, 결합도가 낮다고 판단해 리포 B로 뺀 자료에 다시 적용하면 목적과 수단이 어긋난다.

### 6.5 신규 프로젝트 착수 시 실행 순서 (제안)

1. 리포 A(Core) 생성 — 소스코드 + §1~§5의 이식 대상 거버넌스/설계 문서
2. 리포 B(Reference) 생성 — 착수 즉시 `.gitattributes`로 이미지/PDF/xlsx 등을 lfs 트래킹 대상 지정(§6.3), "1회성 원자료"와 "감사 증적"을 하위 폴더로 구분
3. 리포 A의 `00_GUIDE`에 "리포 B 참조 규칙"(URL 링크 컨벤션, 매니페스트 위치)을 명문화 — 이 §6.4를 그대로 이식
4. 206 방식(Type A/B/C 문서 분류)을 적용하되, "리포 A/B 중 어디에 둘지"를 결합도 기준(§6.1)으로 판단 기준에 추가

---

## 7. Task 관리 체계(R-16/R-17) 이식 가이드 — 도구·스크립트·GitHub/ClickUp 연동

§1.2/§1.3/§1.6은 R-16/R-17을 **규정 텍스트 수준**에서만 다뤘다. Edward 요청(2026-08-04)에 따라 §2(RBAC)·§3(디자인시스템)과 동급으로 **실제 도구·스크립트·GitHub 자동화**를 코드/워크플로우 실사 기준으로 재검토한다.

### 7.1 실사 인벤토리 — 실제로 존재하는 도구

| 자산 | 위치 | 성격 |
|:---|:---|:---|
| 채번 스크립트 | `scripts/next-task-number.sh`, `next-def-number.sh` | `.agent/ACTIVE_TASK.md`+`.agent/tasks/`(또는 `defects/`) 파일명을 **텍스트로 grep해서 최대값+1을 계산**하는 방식 |
| 커밋 해시 삽입 | `scripts/insert-commit-hash.sh` | R-17 완료보고 시 해시 수기 타이핑 오류 방지 |
| 워크트리 격리 | `scripts/agent-worktree-init.sh` | 페르소나별 영속 워크트리 자동 생성/재사용 |
| Task 실행 명령 템플릿 | `opencode.json`의 `check-R17-DoD` 등 | **OpenCode 전용 명령 정의**(Claude Code의 Skill과 다른 메커니즘) — R-17 절차를 프롬프트 템플릿으로 인라인 |
| GitHub Issue → ACTIVE_TASK.md 동기화 | `.github/workflows/sync-issue-to-active-task.yml` | Issue 이벤트(생성/라벨/종료 등) 발생 시 `GH_ISSUES_SYNC` 블록 재생성. Branch protection 때문에 봇이 직접 push 못 하고 **PR만 생성 → Aiden 수동 병합** |
| Issue 레이블 ↔ GitHub Project 상태 동기화 | `sync-label-to-project.yml`(실시간, 이벤트 트리거) + `sync-project-to-label.yml`(수동 `workflow_dispatch`만) | **완전한 양방향이 아니다** — 개인 계정 리포는 `projects_v2_item` 웹훅을 지원하지 않아 역방향(Project→레이블)은 실시간 자동화가 불가능하고 수동 전체 재동기화로 대체돼 있음(Organization 전환 시 해소 예정) |
| Task 파일 템플릿 | 없음 | `.agent/tasks/*.md`는 **정식 템플릿 파일 없이 기존 파일 구조를 관례적으로 복제** — 재사용성 낮음 |

**이식 판정**: 채번 스크립트류는 §7.2의 설계에 따라 이식 여부가 갈린다(그대로 vs 불필요). 워크트리 격리·커밋해시 삽입은 그대로 이식 가치 높음. OpenCode 명령 템플릿은 사용 에이전트 툴체인에 맞게 재구현 필요(Claude Code라면 `.claude/skills/`로). GitHub Actions 동기화 3종은 **패턴 자체(레이블↔상태 필드 매핑, 이벤트 기반 트리거)는 이식 가치 높지만, "개인 계정 vs Organization" 플랜 제약을 신규 프로젝트 착수 전에 반드시 먼저 확인**할 것 — ZENITH_LMS는 이걸 사후에 발견해 회피 설계(수동 dispatch)로 우회했다.

### 7.2 Q1 — GitHub Issue/PR 중심으로 처음부터 간다면 `ACTIVE_TASK.md`가 불필요한가?

> ⚠️ **검증 등급 조정(2026-08-04, 306 §6-1 반영)**: 아래 권장안은 §1.6 채택 원칙("실제 적용 결과가 검증된 것만 채택") 기준으로는 **미검증 제안**이다 — 근거가 현 체계의 실패 이력(Issue #358/#773)뿐이고, 제안된 새 구조 자체가 ZENITH_LMS에서 적용·검증된 적은 없다. **"신규 프로젝트 즉시 적용"이 아니라 "ZENITH_LMS(또는 신규 프로젝트 초기 스프린트)에서 파일럿 적용 → 위반 건수·완료보고 절차 단계 수 등 지표로 검증 → 이식" 등급으로 다룰 것.** Tier 2 이상(§1.7 참조)에서만 참고안으로 검토.

**핵심 근거**: `next-task-number.sh`/`next-def-number.sh`가 존재하는 이유 자체가 "GitHub의 원자적 자동 채번(Issue 번호는 리포 전체에서 절대 중복되지 않음)을 쓰지 않고, `TASK-NNN`이라는 별도 채번 체계를 수동/스크립트로 병행 관리하기 때문"이다. 그 대가로 ZENITH_LMS는 실제 채번 충돌 사고를 반복 겪었다(Issue #358 — TASK 번호 중복, PR#309/313/331/353 등 / Issue #773 — DEF 번호 3연속 충돌). **처음부터 GitHub Issue 번호 자체를 Task ID로 쓰면 이 충돌 자체가 구조적으로 불가능해지고, 채번 스크립트·팀 접두사 체계(R-19)·이 두 Issue가 기록한 사고 이력 전부가 애초에 필요 없어진다.**

다만 로컬 인덱스 파일(`ACTIVE_TASK.md`류) 자체를 완전히 없애는 것과는 별개 문제다 — 남겨야 할 이유:
- R-16이 "세션 시작 시 `ACTIVE_TASK.md` 확인"을 요구하는 이유는 **로컬 파일 1회 읽기가 매 세션·매 에이전트마다 GitHub API를 호출하는 것보다 압도적으로 저렴**하기 때문(ZENITH 기준 Task 453건 규모에서 API 페이지네이션/레이트리밋 부담은 실질적이다)
- "내 팀 담당 중 P1이 몇 건, 뭐가 막혀있나"를 한눈에 스캔하는 용도는 평문 표가 GitHub Issue 목록 필터보다 에이전트 파싱에 유리

**권장안(파일럿 검증 대상)**: 파일을 없애지 말고 **역할을 바꾼다** —
1. Task ID = GitHub Issue 번호 그대로 사용(별도 접두사·채번 스크립트 폐지)
2. `ACTIVE_TASK.md`는 이미 절반은 그렇듯(`GH_ISSUES_SYNC` 블록) **100% 자동생성 read-only 캐시**로 전환 — 지금처럼 "팀별 상세 표"를 별도로 수기 관리하지 않는다. 커밋 해시·테스트 결과 등 상세는 Issue/PR 본문·코멘트에만 존재하게 하고, 인덱스는 그 링크만 가진다.
3. 이렇게 하면 R-17 완료보고 절차가 지금처럼 "task file 🔔 → `ACTIVE_TASK.md` 🔄→🔔 → GH 라벨 변경" **3중 수기 반영**이던 것이 "GitHub Issue/PR 상태 변경 1곳 → 나머지는 자동 파생" 구조로 줄어든다 — 이 3중 반영 자체가 ZENITH_LMS에서 R-17 위반(상태 미변경·해시 미기재)의 상당수가 발생하는 지점이었다.

**남은 주의사항(306 §6-2~6-5 반영)**:
- **즉시성(staleness) 문제**(6-2): 현 sync 워크플로우는 봇이 PR만 생성하고 Aiden이 수동 병합하는 구조라, 병합 전까지 자동생성 캐시는 갱신되지 않는다 — "자동생성"이 곧 "최신"을 보장하지 않는다. 채택 시 병합 지연 완충(미병합 PR 브랜치도 세션 시작 시 함께 조회, 또는 GitHub API 폴백)을 함께 설계할 것.
- **Git 호스트 결합**(6-3): 이 구조는 GitHub Actions/Issue API에 깊게 결합된 선택이다 — §7.5에서 GitLab 등 대안을 검토 중이라면, **Git 호스트를 먼저 확정한 뒤에만** 이 구조 채택 여부를 판단할 것(GitLab 전환 시 sync 워크플로우 전체 재작성 필요).
- **Issue/PR 번호풀 공유 및 에픽 매핑**(6-5): GitHub은 Issue와 PR이 같은 번호풀을 공유하므로 Task ID=Issue 번호는 PR 번호와 혼동 여지가 있다. 또한 "1 Issue = 1 Task" 가정은 에픽성 Issue(다중 작업 단위를 묶는 Issue)에서 무너진다 — 에픽/스프린트 단위를 쓰는 체계라면 Task↔Issue↔PR 매핑 규칙을 별도 정의할 것.

### 7.3 Q2 — ClickUp 연동 방안

- ClickUp은 **네이티브 GitHub 연동**을 제공한다(브랜치명/커밋메시지/PR 제목에 ClickUp Task ID를 참조하면 자동 링크, PR 머지 시 Task 상태 자동 전환). 단순 연동은 이 기본 기능으로 충분 — 커스텀 코드 불필요.
- 더 세밀한 자동화(예: `team:x` 라벨이 붙은 GitHub Issue가 열리면 자동으로 ClickUp Task 생성)가 필요하면, ZENITH_LMS가 이미 `sync-label-to-project.yml`로 검증한 패턴(이벤트 트리거 → 외부 API 호출)을 그대로 재사용 — 호출 대상만 GitHub GraphQL(Project V2)에서 ClickUp REST API로 바꾸면 된다.
- **양방향 실시간 동기화는 처음부터 만들지 말 것을 권장**한다 — ZENITH_LMS가 정확히 이 패턴(Project 상태 ↔ Issue 레이블 양방향)을 시도하다가 "개인 계정 리포는 역방향 웹훅 미지원"이라는 플랫폼 제약에 부딪혀 결국 수동 dispatch로 물러난 전례가 있다(§7.1). GitHub↔ClickUp도 유사한 플랜/권한 제약에 부딪힐 가능성이 높으므로, **단방향(GitHub Issue 생성 → ClickUp Task 자동 생성/상태 갱신)으로 시작하고, 역방향이 실제로 필요해졌을 때만 검증 후 추가**한다.
- **조건부 판정(306 §6-6)**: "네이티브 연동으로 충분"이라는 위 판정은 ClickUp의 **플랜별 기능 제약**(연동 가능 리포 수·양방향 지원 여부·브랜치/커밋 참조 범위)을 검증하지 않은 낙관적 판정이다 — 실제 채택 전 플랜 문서에서 연동 범위를 먼저 확인하고, 부족분만 위 sync 패턴으로 보완할 것.

### 7.4 Q3 — `ACTIVE_TASK.md` · GitHub Issue/PR · ClickUp 상호 연계 및 운영안

세 시스템에 **동일 상태값을 두 곳 이상이 "쓰기 권한"으로 갖지 않도록** 레이어별로 소유권을 하나씩만 지정한다(양방향 실시간 동기화는 드리프트의 근원이라는 게 §7.1의 Project↔레이블 사례로 이미 실증됨):

| 레이어 | 시스템 | 역할 | 쓰기 권한 |
|:---|:---|:---|:---|
| **1. 엔지니어링 실행 기록(System of Record)** | GitHub Issue + PR | "코드 작업이 실제로 됐는지/리뷰됐는지/머지됐는지"의 유일한 진실 | 에이전트·팀 리더·Aiden |
| **2. 로컬 에이전트 캐시(속도 전용)** | `ACTIVE_TASK.md` | R-16 세션 시작 시 빠른 로컬 조회 — §7.2 권장안대로 100% 자동생성(파일럿 검증 후) | 없음(자동생성 전용, 수기 편집 금지) |
| **3. 사업/PM 뷰** | ClickUp | GitHub Issue로부터 단방향 파생 + PM 전용 필드(사업 우선순위·일정·리소싱)로 보강 | PM/팀 리더(PM 전용 필드만), 엔지니어링 상태는 읽기전용 |

이 구조면 이전에 검토한 사업관리 보고서(진척/위험/주간/월간, §07_Project_Management 논의)도 ClickUp 대시보드에서 자동 산출 가능해져, 수기 보고서 작성 부담이 줄어든다.

**잔여 위험(306 §6-7)**: 위 원칙은 **레이어 간**에는 지켜지지만, 레이어 1(GitHub Issue) 내부는 에이전트·팀 리더·Aiden이 모두 쓰기 권한을 가져 이벤트 레이스(상태 충돌) 여지가 남는다 — 상태 전이는 사람이 아니라 **라벨 전이 검증 자동화**(불법 전이 차단)로 중재하고, "시스템 단일 소스 + 사람 단일 소유" 원칙을 명문화할 것.

### 7.5 Q4 — 사내(on-premise)/local Git 플랫폼 운영 검토

현재 GOV_COMMON.md/R-16~R-20 전체가 `gh` CLI·GitHub Actions·GitHub API에 강하게 결합돼 있다는 전제 위에서 비교한다.

| 옵션 | 마이그레이션 비용 | 비용 | 인프라/운영 부담(306 §6-4) | 에이전트 자동화 호환성(306 §6-4) | 적합 조건 |
|:---|:---|:---|:---|:---|:---|
| **GitHub Enterprise Server(자체호스팅)** | **낮음** — `gh` CLI/API/Actions가 사실상 동일하게 동작, 기존 R-16~R-20 자동화 그대로 이식 가능 | 높음(엔터프라이즈 라이선스) | 높음(HA 운영, **셀프호스티드 러너 별도 구축·네트워크 허용목록 운영 필요** — GitHub.com 호스팅 러너 사용 불가) | 높음(사전 확인 조건부 — §1.5와 동일 원칙: 자체호스팅에서도 `gh`/Actions가 동일하게 동작하는지 검증 필요) | 사내망 격리·데이터 레지던시가 목적이고, 기존 GitHub 기반 자동화를 그대로 유지하고 싶을 때 |
| **GitLab(자체호스팅 CE/EE)** | **높음** — `gh`→`glab`, GitHub Actions→GitLab CI YAML 재작성, 이 문서의 R-16~R-20 자동화 전부 재구현 필요 | CE는 무료 | 높음(자체 러너·인프라 운영) | 낮음(자동화 전면 재구현 전제) | 비용이 우선이고 워크플로우 재작성 리소스가 있을 때. 성숙한 자체호스팅 생태계 |
| **Gitea/Forgejo** | 중간(API가 GitHub과 유사해 일부 호환) | 매우 낮음(경량) | 낮음(경량 인프라) | 낮음(Actions류 CI 성숙도 낮음) | 소규모·저예산. 다만 이 프로젝트처럼 자동화 의존도가 높은 워크플로우엔 비권장 |

**우선 확인할 것**: "사내 운영"이 요구하는 게 실제로 무엇인지 먼저 명확히 할 것 — ① 데이터 레지던시/네트워크 격리가 목적이면 GitHub Enterprise Server가 마이그레이션 비용 최소, ② 순수 비용 절감이 목적이면 GitLab CE, ③ 만약 목적이 "접근 통제"뿐이라면 굳이 자체호스팅 없이 **GitHub.com Private repo + SSO/IP allowlist**만으로 해결될 수도 있음(마이그레이션 비용 0) — 이 경우가 아닌지 먼저 배제할 것.

**Public vs Private 리포 차이(306 §D-3)**: 현재 `GH_PROJECT_TOKEN`(Admin PAT) + 봇 PR·수동 병합 구조(§7.1)는 **Public 리포 기준**으로 만들어졌다. Private/Enterprise 리포는 branch protection·Actions 기본 권한 동작이 달라 워크플로우 세부 조정이 필요하다 — 사내 전환 시 이 차이를 위 비교와 함께 반드시 재검증할 것.

### 7.6 자율/반자율 운영 시 에이전트 간 통신 방식 — Polling vs Event-Driven

Edward 요청(2026-08-04)에 따라 검토한다: §7.2~§7.5가 다룬 Task 관리 자동화는 "상태를 어디에 기록할 것인가"였다면, 이 절은 그 상태 변화를 **에이전트가 서로 어떻게 알아채는가**를 다룬다. "같은 오케스트레이터 안의 sub-agent"와 "서로 다른 독립 세션(페르소나)" 두 경우가 성격이 완전히 다르므로 나눠서 본다.

**A. 같은 오케스트레이터 안의 sub-agent** — 이미 해결된 문제, 별도 설계 불필요. 백그라운드 실행 완료 시 자동 알림(폴링이 아니라 event-driven), 스폰된 에이전트에게 메시지를 보내 재개시키는 양방향 채널, 백그라운드 프로세스의 출력을 스트리밍하는 이벤트 구독, 파이프라인 단계 간 결과를 `await`로 연결해 폴링 없이 의존성을 해소하는 구조 — 이 네 가지 원시 기능만으로 오케스트레이터-서브에이전트 통신은 충분하다. 신규 프로젝트에서 사용할 에이전트 툴체인이 이 네 기능(비동기 완료 알림·재개 메시징·이벤트 스트리밍·파이프라인 의존성 해소)을 제공하는지만 착수 전 확인하면 된다.

**B. 서로 다른 독립 세션(ZENITH의 Team A/B 같은 구조) — 진짜 어려운 케이스**. ZENITH_LMS가 이미 실제로 구축·검증한 답이 있다:
- **GitHub Issue/PR을 공유 블랙보드로, GitHub Actions를 이벤트 트리거로 사용**하는 패턴이 `sync-issue-to-active-task.yml`/`sync-label-to-project.yml`(`on: issues: types: [opened, labeled, ...]`)로 실증되어 있다 — 한 에이전트가 Issue 라벨을 바꾸면 다른 에이전트(또는 사람)가 이벤트로 즉시 인지한다.
- **단, 순수 event-driven만으로는 부족함도 이미 실측됐다**(§7.1) — 개인계정 리포가 `projects_v2_item` 웹훅을 지원하지 않아 역방향 자동화가 막혔고, 결국 `workflow_dispatch`(수동/주기적 전체 재동기화)로 대체됐다. 이는 사실상 "이벤트가 유실됐을 때를 대비한 폴링 백업"이다.

**권장 패턴 — "Event-driven 우선 + 주기적 재동기화(polling) 백업"**: 이는 임기응변이 아니라 분산시스템의 표준 패턴이다(Kubernetes controller의 watch+periodic reconcile 루프와 동일 원리) — 이벤트가 안정적으로 전달되는 경로는 즉시 반응(저지연), 이벤트가 유실되거나 플랫폼이 미지원하는 경로는 주기적 전체 재확인으로 안전망을 둔다. ZENITH_LMS가 이 형태로 귀결된 것은 우연이 아니라 이 문제의 자연스러운 해법이기 때문이다 — 신규 프로젝트도 착수 시점부터 "이벤트 우선, 폴링은 안전망"으로 설계할 것을 권장한다.

**C. 더 세밀한 핸드오프가 필요할 때 — 공유 상태/큐 옵션**: GitHub 이벤트만으로 부족하면(순서 보장, 세밀한 상태 전달 등) Supabase/Postgres의 `LISTEN/NOTIFY`(진짜 push) 또는 별도 큐 테이블을 추가할 수 있다. 다만 이는 §7.4의 "필드별로 소유 시스템 하나만" 원칙과 충돌하지 않도록 **GitHub Issue 상태를 여전히 유일한 진실로 두고, 이 큐/알림 테이블은 그 이벤트를 배달하는 수단으로만 역할을 제한**해야 한다 — 그러지 않으면 진실의 원천이 두 곳으로 갈라져 §7.4가 막으려던 드리프트가 다시 생긴다.

### 7.7 WBS ↔ ClickUp 매핑 — 공정관리·공정률 관리 방안 (스택/도메인 무관, 두 프로젝트 공통 적용)

Edward 요청(2026-08-04)에 따라 정리한다. 리포지토리의 WBS 문서(계층적 작업분류·담당·산출물·기간)를 ClickUp Task와 매핑해 공정관리·공정률(진행률)을 관리하는 방안이다. WBS 구조 자체(10진법 ID 체계 `Phase.Group.Task` 또는 체크박스 상태코드 방식)는 스택·도메인과 무관하므로, 어떤 신규 프로젝트에도 그대로 적용 가능하다.

**3계층 분리 — 구조 기준선 / 실시간 진행 / 산출 리포트**:

1. **Layer 1 — WBS 기준선(저장소 마크다운이 원본)**: WBS 문서(10진법 ID, 담당 역할, 산출물 경로)는 §1.4의 GUIDE(잘 안 바뀌는 표준)에 해당 — 스코프 변경 시에만 "리베이스라인"이라는 통제된 절차로 갱신한다.
2. **Layer 2 — ClickUp(실시간 진행 상태의 원본)**: WBS 계층을 ClickUp 구조로 1회성 임포트(Phase→List, Group→상위Task, Task→Subtask). 커스텀 필드로 `WBS_ID`(텍스트)·`담당역할`(드롭다운)·`산출물경로`(텍스트/링크)·`가중치`(숫자, 예상 MD/스토리포인트)·`%완료`(진행률 필드)를 둔다 — ClickUp 네이티브 필드만으로는 WBS 고유 정보를 못 담는다.
3. **Layer 3 — 공정률 가중 롤업**: 단순 "완료 Task 수/전체 Task 수"가 아니라 **가중 평균**으로 계산할 것 — `Phase 진행률 = Σ(Task_i.가중치 × Task_i.%완료) / Σ(Task_i.가중치)`. ClickUp 네이티브 서브태스크 진행률은 보통 단순 카운트 기반이라 이 계산까지는 못 한다 — API로 주기적으로 가져와 계산하는 스크립트/대시보드가 필요하며, 이는 §7의 사업관리 보고서 자동생성 논의와 같은 파이프라인으로 재사용 가능하다.

**실제 개발 작업과의 연결(R-08 원칙 적용)**: 코드 산출물에 해당하는 WBS Task는 담당자가 수동으로 "100%"를 클릭하게 두지 말고, ClickUp의 GitHub 연동(§7.3)으로 실제 PR 머지 시 자동 완료 전환되게 할 것 — "자가보고 진행률은 증거로 불인정, 실제 완료 신호만 인정"이라는 R-08 원칙을 공정률에도 동일 적용한다. 설계·문서 산출물처럼 코드가 아닌 Task는 담당자 수동 갱신으로 남긴다.

**동기화 방향 — 단방향, 실시간 양방향 금지**: WBS 기준선 → ClickUp은 1회성(또는 리베이스라인 시에만) 임포트. ClickUp → 저장소 WBS 마크다운은 Phase 게이트·마일스톤 시점마다 주기적 스냅샷으로 진척도 대시보드 표를 갱신 — §7.3/§7.4가 이미 확립한 "실시간 양방향 동기화는 드리프트의 근원" 원칙을 그대로 적용한다.

**ZENITH_LMS 실제 사례로 구체화**: `docs/01_WBS/WBS_01_상세_공정표.md`를 기준으로 위 매핑을 실제 데이터에 적용하면 다음과 같다.

- **계층 매핑(4단계)**: L1 `Phase 1: Identity & Foundation` → ClickUp List / L2 `1.1 [Infra & DB] 기초 환경 구축 (8 MD)` → 상위 Task / L3 `1.1.1 프로젝트 초기화 및 CI/CD (2 MD)` → Task / L4 `1.1.1.1 Next.js(FE) & Supabase 연결 설정 (1 MD)` → Subtask(leaf, 실제 `%완료` 갱신 단위).
- **`가중치` 필드는 이미 WBS 텍스트에 내재**: ZENITH WBS는 모든 레벨에 `(N MD)`가 이미 붙어 있다(`1.1.1.1 ... (1 MD)`) — 임포트 스크립트가 이 괄호값을 파싱해 ClickUp `가중치` 커스텀필드에 그대로 채우면 되고, 별도로 가중치를 새로 산정할 필요가 없다.
- **7종 상태코드 → ClickUp `%완료`/상태 매핑**:

  | ZENITH 상태코드 | 의미 | ClickUp 매핑 |
  |:---:|:---|:---|
  | `[x]` (완료) | 검증까지 완료 | `%완료`=100, Status=Done |
  | `[/]` (진행) | 수행 중 | `%완료`=담당자 수기 추정치(0~99), Status=In Progress |
  | `[ ]` (대기) | 시작 전 | `%완료`=0, Status=Open |
  | `[!]` (재작업) | 검증 실패/요건변경 | `%완료`를 100→재추정치로 되돌리고 Status=Rework(별도 라벨) — 가중 롤업 계산에서 "완료했다가 되돌아간 것"이 드러나야 함 |
  | `[-]` (Skip) | 영구 제외 | 가중 롤업의 분모(Σ가중치)에서 **제외** — 포함하면 진행률이 실제보다 낮게 왜곡됨 |
  | `[>]` (Deferred) | 의도적 이월 | 현재 Phase 롤업 분모에서 제외, 재개 예정 Phase의 분모에 포함 |
  | `[+]` (추가) | 신규 발생 | 발견 즉시 분모에 추가(사전 계획에 없던 항목이므로 Phase 전체 가중치 합이 변경됨을 인지할 것) |

**Task-산출물 매핑은 선택이 아니라 필수(Edward 지시, 2026-08-04)** — 모든 leaf Task는 `산출물경로` 커스텀필드가 채워진 상태로만 "완료" 전환이 허용되어야 한다. Task ID·가중치·%완료만 관리하고 산출물을 비워두면, "무엇을 했다고 진행률을 올렸는지"를 사후에 검증할 방법이 없다 — 이는 R-08("자가보고는 증거 불인정")의 산출물 버전이다.

- **G_Toll_Platform_clone은 이미 구조화돼 있어 그대로 매핑**: `Docs/00_Plan/10_WBS/107_WBS_Phase4_Detail.md` 등은 Task 행마다 "산출물 (실제 파일 경로)" 컬럼이 이미 있다(예: `4.1.3 | 서비스 태스크 핸들러 구현 | Backend | JobWorker.java | ⏳`) — 임포트 시 이 컬럼을 그대로 `산출물경로`에 넣으면 된다.
- **ZENITH_LMS는 실측 결과 이 구조가 없다**: `WBS_01_상세_공정표.md`의 "비고" 필드는 총 160건 중 **7건(4%)만** 실제 파일 경로(`.ts`/`.tsx`/`.sql`)를 언급하고, 나머지는 "완료"·"계층 구조 반영"·"트리거 구현" 같은 비구조적 서술이다. 즉 ZENITH WBS를 그대로 ClickUp에 임포트하면 산출물 필드가 대부분 공란이 된다.
  - **과거 완료분(Phase 1~5)**: 소급 채우기는 비용 대비 효과가 낮다 — 완료된 항목이므로 공란으로 두거나, 필요시 GitNexus `context`/`impact`로 해당 Task 설명과 매칭되는 실제 심볼·파일을 사후 역추적해 채울 것(예: "1.1.1.1 Next.js(FE) & Supabase 연결 설정"이라면 `src/lib/supabase/*` 관련 파일을 GitNexus로 조회해 매핑).
  - **향후 신규 Task(Phase 6/7 이후)**: WBS 작성 시점부터 산출물 컬럼을 G_Toll 방식처럼 구조화된 필드로 강제할 것 — "비고에 가끔 적는다"가 아니라 "Task 정의 시 산출물 경로가 없으면 Task 자체가 미완결"로 취급.
- **검증 강화 — 산출물 경로는 텍스트가 아니라 검증 대상**: `산출물경로`를 자유 텍스트로 두면 실제로 그 파일이 존재하는지, 정말 그 Task가 만든 게 맞는지 확인할 길이 없다. 두 가지를 병행할 것:
  1. **존재 검증**: 임포트/갱신 시점에 스크립트가 `산출물경로`에 적힌 경로가 리포에 실제로 존재하는지 확인(단순 파일 존재 체크, 또는 GitNexus `context({name})`으로 해당 심볼이 실재하는지 조회 — §1.5-a에서 다룬 영향도 분석 용도와 동일 도구 재사용).
  2. **완료-신호 검증 — 단일 신호로는 부족, 다층 신호가 필요(2026-08-04 Edward 문제제기로 보강)**: "PR diff가 산출물 경로를 포함하는가"만으로는 "파일에 손을 대긴 댔다"만 증명될 뿐, "제대로 완료했다"는 증명되지 않는다(210 §2의 vacuous-test 안티패턴과 같은 함정 — 파일은 건드렸지만 요구사항은 미충족). 이 프로젝트가 이미 확립한 신호를 겹쳐 써야 실질적 검증이 된다:
     - **① 코드 신호**: PR diff가 `산출물경로`를 포함하는가(필요조건)
     - **② 테스트 신호(R-08)**: 그 PR의 CI(회귀테스트)가 실제로 통과했는가 — 자가보고 아닌 CI 실행결과로만 판단
     - **③ UI 증적 신호(R-10)**: 산출물이 사용자 대면 기능이면, 백엔드 PR 머지만으로 완료 처리하지 말고 실제 UI 구동 스크린샷/증적 첨부 여부까지 확인 — "백엔드 완료≠기능완료" 원칙 그대로 적용
     - **④ "머지됨" 자체가 이미 프로세스 게이트임을 활용**: 이 프로젝트 R-17 절차상 PR 머지는 이미 리뷰+CI통과를 내포한다 — 따라서 "아무 PR이 Task ID를 언급"이 아니라 **"실제로 머지된 PR"이 산출물 경로를 포함하는가**로 조건을 좁히면, 프로세스 차원 보증(①+②)과 내용 차원 보증(③)을 함께 얻는다
     - 네 신호를 전부 갖춰야 자동 완료 전환을 허용하고, 하나라도 빠지면 사람이 확인하는 수동 완료로 남길 것 — ①만으로 자동완료를 허용하면 "자가보고보다는 낫지만 실질적 검증은 아닌" 중간 상태에 머무른다.
- **부가 가치 — 역방향 조회**: 산출물이 제대로 매핑되면 "이 파일을 고치면 어느 WBS Task/Phase에 영향을 주는가"를 역으로 조회할 수 있게 된다 — 이는 GitNexus의 `impact` 분석(§1.5-a)과 자연스럽게 결합된다: 코드 수정 전 `impact`로 영향받는 파일을 확인하고, 그 파일이 매핑된 WBS Task가 이미 "완료"로 표시돼 있다면 상태를 재검토해야 한다는 신호로 쓸 수 있다.

- **"잔여 MD" 컬럼도 같은 공식에서 자연히 도출됨**: ZENITH 대시보드가 이미 쓰는 "잔여 MD" 컬럼은 `Σ(Task_i.가중치 × (1 − Task_i.%완료))`로, 위에서 정의한 가중 롤업 공식의 부산물이다 — 별도 계산식이 아니라 같은 데이터에서 진행률(%)과 잔여MD(MD)를 동시에 뽑아내면 된다. 즉 ZENITH의 "📊 프로젝트 진척도 대시보드"(Phase\|목표\|진행률(%)\|잔여MD\|상태 5열 표)는 ClickUp 임포트 후 스크립트가 그대로 재생성할 수 있는 리포트다.

---

## 8. 애플리케이션 코드 자산 이식 판정 (307/308 종합 검증)

307(Riley, CPO 관점)이 305/306이 다루지 않은 **4대 애플리케이션 재사용 자산**을 제안했고, 308(D_Kai, Implementer 관점)이 §1.6 채택 원칙에 따라 코드 실사로 재검증했다. 아래는 그 결과를 §1.6 기준으로 정리한 최종 판정이다 — **4건 중 2건은 등급이 하향되고, 1건은 "그대로 이식"이 아니라 "재설계" 항목으로 재정의된다.**

| 자산 | 307(CPO) 원 판정 | 308(Implementer) 코드 실사 결과 | 최종 판정 |
|:---|:---|:---|:---|
| **A. Server Action 표준 응답 + Zod 가드**(`validateUserAction()`) | 그대로 이식 (VERY HIGH) | 함수는 실재(`src/lib/auth/guards.ts:120`)하나, **가드 함수가 있어도 AGENCY 역할 누락 사고가 8회 이상 반복**됐다(DEF-114/116/117 등, §2.3) — "가드가 있다"는 게 "누락이 안 생긴다"를 보장하지 않음이 이미 실증됨 | **이식하되 §2.3(4계층 일치)·§2.4(실브라우저 검증) 요건과 반드시 1:1 결합** — 가드 함수 단독 복사는 반쪽 이식 |
| **B. i18n 서버/클라이언트 에러코드 분리**(`ERR_*` 코드) | 패턴 개선 이식 (HIGH) | 코드베이스 전체에 `ERR_AGENCY_REVERSE_MARGIN` 류 에러코드가 **어디에도 존재하지 않음** — 서버 액션 50개 이상이 한글 인라인 문자열을 그대로 반환 중(`ups-actual-charges.ts:39` 등) | **"이식 대상"이 아니라 "신규 설계 제안"으로 재라벨링** — 반증(한글 인라인 반환의 다국어 확장 한계, §4.1 DEF-115/061)은 명확하나 제안 자체는 미검증. 신규 프로젝트 최초 구현 시 에러코드 네임스페이스를 새로 설계할 것 |
| **C. 범용 상태머신 엔진**(`canChangeStatus()`) | 도메인 분리 이식 (VERY HIGH) | 실재(`src/lib/logistics/status-machine.ts:46`)하나 물류 전용 `OrderStatus` **13개**(`src/types/orders.ts:8` 실측 — REGISTERED~MASTERED) enum에 전면 결합, "감사로깅 자동 포함" 주장은 검증 결과 부정확(`status-machine.ts`에는 history/insert 코드 없음 — 실제로는 `order.repository.ts:203` `insertStatusHistory()`를 호출부가 수동 호출), §4.5가 이미 지적한 변형 축(`transport_mode`) 누락 — DEF-B-031~033 연쇄의 근본원인 중 하나 | **"그대로 이식" 아님 — "제네릭 상태머신 재설계" 항목.** 재설계 시 ①제네릭 열거·전이맵 추출 ②감사 로깅 내장 ③변형 축(역할×변형×상태전이 트리플) 3가지를 함께 명세할 것 |
| **D. 아티팩트 기반 UI 승인 워크플로우** | 방법론 이식 (HIGH), "재작업 80% 감축 실증" | 해당 측정 근거(세션·Task·지표)가 저장소 어디에도 없음 | **미검증 제안** — 신규 프로젝트 첫 스프린트에서 도입 후 재작업 건수를 실제 측정하는 파일럿으로만 채택 |

**정리**: 이식 판정은 "그대로 가져가서 쓰는 것"과 "참고해서 새로 만드는 것"을 구분해야 한다 — 자산 A만 (조건부) 그대로 이식이고, B·D는 신규 설계·파일럿 대상, C는 참고 패턴 기반 재설계 대상이다.

---

## 9. 신규 저장소 착수 체크리스트 (통합)

1. [ ] GOV_COMMON.md 대응 파일 작성 — §1.2 이식표 기준으로 R-규정 포팅, ZENITH 고유 사건번호·인물명 제거
2. [ ] R-06/R-20 등 MCP 기반 규칙 채택 전, 참여할 모든 에이전트 실행환경에 해당 MCP가 실제로 연결되는지 확인(§1.5)
3. [ ] R-17 반복위반 페널티를 문서에 넣는다면, 실제 집행 여부에 대한 팀 합의를 먼저 확정(§1.3)
4. [ ] RBAC 스캐폴드(§2) 이식 — 역할/경로 전면 재정의 + 304 문서 §4.3(단일 소스 코드생성) 방향 함께 검토
5. [ ] 디자인 시스템(§3) 이식 — 컴포넌트 그대로, 토큰/레이아웃 상수 분리
6. [ ] 다국어(§4.1) — 라우팅 골격 이식 + 네임스페이스 소유권 컨벤션 + 로케일 전환 UI 추가
7. [ ] 통화(§4.2) — 표시 컴포넌트만 재사용, 변환 로직은 중앙 단일 함수로 새로 설계
8. [ ] 테마(§4.3) — 기존 방식 이식하지 말고 ThemeProvider+시맨틱 토큰으로 재설계
9. [ ] 커스텀필드·워크플로우 엔진 필요 시 304 문서 §4.4/§4.5 원칙부터 적용(§4.4, §4.5)
10. [ ] Seed 멱등성 패턴·Rate Limiting·알림 래퍼·API 감사로그(§5.1)는 그대로 복사
11. [ ] CI 파이프라인·파일 스토리지 패턴·배포 SOP(§5.2)는 프로젝트별 치환 후 사용
12. [ ] 210 가이드 §5(신규 프로젝트 착수 체크리스트)·304 가이드 §6(적용 가이드) 그대로 적용
13. [ ] 210/304의 "실증 섹션"은 각주로만 남기고, "Part B/§6 이후"만 실제 규정으로 채택
14. [ ] 리포 A(Core)/리포 B(Reference) 2-리포 분리 여부 결정(§6.1·§6.2) — 결합도 기준으로 문서 배치 판단
15. [ ] 리포 B 채택 시 착수 즉시 `.gitattributes` 설정(§6.3) — 나중에 히스토리 재작성하는 상황 예방
16. [ ] 리포 A `00_GUIDE`에 리포 B 참조 규칙(URL 링크 컨벤션 + 매니페스트 위치) 명문화(§6.4)
17. [ ] Task ID=GitHub Issue 번호 + ACTIVE_TASK 자동생성 캐시(§7.2)는 **즉시 채택하지 말고 Tier 2 이상에서 파일럿 검증 후 이식** — 미검증 제안임을 인지할 것(306 §6-1)
18. [ ] §7.2 파일럿 채택 시 병합 지연으로 인한 캐시 staleness 완충 설계(§7.2, 306 §6-2) + Git 호스트 확정 이후에만 판단(306 §6-3) + Issue/PR 번호풀 공유·에픽 매핑 규칙 별도 정의(306 §6-5)
19. [ ] GitHub Actions 이벤트 기반 동기화(§7.1) 설계 전 GitHub 플랜(개인 계정 vs Organization) 제약 확인 — `projects_v2_item` 웹훅 등 일부 이벤트는 Organization 전용
20. [ ] ClickUp 등 PM 도구 연동 시 네이티브 GitHub 연동으로 시작(단, 플랜별 기능 제약 사전 확인, 306 §6-6), 양방향 실시간 동기화는 단방향 검증 후 필요할 때만 추가(§7.3) + 레이어1 내부 다중 쓰기 레이스는 라벨전이 자동화로 중재(306 §6-7)
21. [ ] 사내/자체호스팅 Git 플랫폼 검토 시 "목적"(데이터 레지던시 vs 비용 vs 접근통제)을 먼저 확정 + 인프라·에이전트 자동화 호환성 축도 함께 비교(§7.5, 306 §6-4) + Public/Private 리포 CI·봇 권한 차이 재검증(306 §D-3)
22. [ ] 프로젝트 규모(1~2인/소수·멀티에이전트/다중팀)에 따라 Tier 1~3 중 채택 스코프 결정 — 전체 거버넌스가 항상 정답은 아님(§1.7)
23. [ ] RBAC 스캐폴드 이식 시 "역할 한정 화면 실브라우저 검증" 요건을 반드시 함께 이식(§2.4)
24. [ ] R-17 위반 페널티는 기록(VIOLATION_TRACKER)만으로 두지 말고 PR 차단 등 자동 게이트와 연결(§1.3)
25. [ ] 에이전트 툴체인이 비동기 완료 알림·재개 메시징·이벤트 스트리밍·파이프라인 의존성 해소 4가지를 지원하는지 확인(§7.6-A) — 서로 다른 세션 간 통신은 "이벤트 우선 + 주기적 재동기화" 하이브리드로 설계(§7.6-B), 공유 큐 추가 시 진실의 원천을 GitHub Issue 하나로 제한(§7.6-C)
26. [ ] GitNexus(§1.5-a) 채택 시 Tier 2 이상에서만, "도구 + 인덱스 freshness 검증 절차 + 전 에이전트 환경 연결 확인" 3종 세트로 이식 — 도구만 복사하지 말 것
27. [ ] 애플리케이션 코드 자산(§8) 이식 시 표의 최종 판정 등급 그대로 적용 — 특히 자산 B(i18n 에러코드)·D(UI 아티팩트 워크플로우)는 "이식"이 아니라 "신규 설계/파일럿 측정" 대상임에 유의
28. [ ] `check-db-freshness.sh`(R-08-2, 306 §A-3) — 로컬 DB가 CI(매번 fresh reset) 대비 stale해질 수 있음을 인프라 셋업 단계(로드맵 1단계)에서부터 반영

---

## 개정 이력

| 버전 | 날짜 | 작성자 | 설명 |
| :--- | :--- | :--- | :--- |
| v1.0 | 2026-07-31 | Aiden (Claude, ZEN_CEO) | 최초 작성 — Edward 지시(신규 저장소 기준 재활용 가이드, 실제 이식 작업 미수행). 거버넌스/방법론(R-00~R-20 이식표 포함)·RBAC 스캐폴드·디자인 시스템 3개 영역 다룸. 2026-07-31 기준 유효 절차만 인용(스테일 버전 제외) |
| v1.1 | 2026-07-31 | Aiden (Claude, ZEN_CEO) | §4 신설 — Edward 요청(다국어·통화·화면 Skin 적용기법 + 커스텀필드·워크플로우 검토사항 포함). 코드 실사 기준 다국어(i18n)·통화·테마 성숙도 평가(전부 "패턴 참고, 구현 보강 필요" 등급), 커스텀필드/워크플로우는 304 문서 §4.4/§4.5 교차참조. 체크리스트에 항목 6개 추가 |
| v1.2 | 2026-07-31 | Aiden (Claude, ZEN_CEO) | §5 신설 — Edward 요청("기타 추천 항목"). CI 파이프라인·테스트 3계층 구조·Seed 멱등성 패턴·Rate Limiting·외부 API 감사로그·파일 스토리지·배포 SOP 8개 항목 코드 실사, 그대로 이식 가능 4건(Seed 패턴/Rate Limiting/알림 래퍼/API 감사로그) + 조정 필요 3건(CI/파일스토리지/배포SOP) 판정. 체크리스트에 항목 2개 추가 |
| v1.3 | 2026-07-31 | Aiden (Claude, ZEN_CEO) | §1.6 신설 — Edward 지시("실제 적용 결과가 의미 있는 항목만 채택"이라는 원칙 명문화). 문서/폴더 관리(206)·Multi-Agent 운영(104/107) 후보를 이 기준으로 재검토 — VIOLATION_TRACKER.md만 조건부 실증(가시화엔 효과, 재발방지 단독으론 반증)으로 채택, 나머지 5개 후보(수동 문서번호 확인 컨벤션·Obsidian Vault·107 스케일링 제안서·104 R&R 가이드·SQL 스크립트 관리 규칙)는 실증 부족으로 미채택 — 사유와 함께 투명하게 기록 |
| v1.4 | 2026-08-04 | Aiden (Claude, ZEN_CEO) | §6 신설 — Edward 요청(신규 프로젝트 착수 대비 리포지토리 구조 방안, ZENITH_LMS 자체 재구성 아님). ZENITH_LMS 실측 근거(docs/ 바이너리 59MB·.git 143MB·git-lfs 미설정으로 스크린샷 7~8버전 누적) 기반, 분리 기준은 "바이너리/변경빈도"가 아니라 "코드와의 결합도"(R-12/R-04 원자적 커밋 요건)임을 명문화. 2-리포(Core/Reference) 분리 구조, 리포 B 바이너리 관리(자료 성격별 git-lfs 적용 여부 구분), 상호참조 방식(Submodule/Subtree 비권장, 느슨한 링크+매니페스트 권장) 제시. 체크리스트에 항목 3개 추가 |
| v1.5 | 2026-08-04 | Aiden (Claude, ZEN_CEO) | §7 신설 — Edward 요청(Task 관리 체계를 §2/§3과 동급으로 도구·스크립트·GitHub 자동화 실사). `next-task-number.sh`/`next-def-number.sh`(텍스트 grep 기반 채번, Issue #358/#773 충돌 이력의 근본원인)·`sync-issue-to-active-task.yml`·`sync-label-to-project.yml`/`sync-project-to-label.yml`(개인계정 리포는 `projects_v2_item` 웹훅 미지원으로 역방향 실시간 자동화 불가, 수동 dispatch로 대체돼 있음을 확인) 등 실제 자산 인벤토리화. Q1(GitHub Issue 번호를 Task ID로 직접 쓰면 별도 채번체계 자체가 불필요해진다는 판정 + ACTIVE_TASK.md는 폐기가 아니라 "100% 자동생성 read-only 캐시"로 역할 전환 권장), Q2(ClickUp 연동은 네이티브 GitHub 연동으로 시작, 양방향 실시간 동기화는 ZENITH가 겪은 플랫폼 제약을 근거로 비권장), Q3(ACTIVE_TASK.md/GitHub Issue-PR/ClickUp 3계층 소유권 분리 모델), Q4(GitHub Enterprise Server/GitLab CE/Gitea 비교, 목적(레지던시/비용/접근통제) 우선 확인 권고) 정리. 체크리스트에 항목 5개 추가 |
| v1.6 | 2026-08-04 | Aiden (Claude, ZEN_CEO) | §8 신설 — B_Kai가 작성한 별도 검토 의견 문서(306_NEW_PROJECT_REUSE_REVIEW.md) 개요·링크를 문서 상단에 추가. 306은 이식 후 운영·검증 방식과 신규 도메인 테스트 자산 시작점을 보완하며, B/D 항목은 본 문서 §7과 정합됨 |
| v1.7 | 2026-08-04 | Aiden (Claude, ZEN_CEO) | §8 개요 갱신 — 306이 v1.1로 확장되어 §6(본 문서 §7 v1.5 권장안에 대한 검토 이견 6-1~6-7)이 추가됨을 반영. §7.2 "Issue 번호 = Task ID + ACTIVE_TASK 자동생성" 권장안에 대해 306 §6에서 305 §1.6 채택 원칙(실제 적용 결과 검증) 위반의 미검증 제안으로 이견 표명 |
| v1.8 | 2026-08-04 | Aiden (Claude, ZEN_CEO) | Edward 요청(306 검토 의견 본문 반영). §7.2를 "즉시 권장"에서 "Tier 2 이상, 파일럿 검증 후 이식" 등급으로 하향(306 §6-1)하고 캐시 즉시성(6-2)·Git 호스트 결합 모순(6-3)·Issue/PR 번호풀 공유·에픽 매핑(6-5) 주의사항 추가. §7.3에 ClickUp 플랜 제약 조건부 판정(6-6), §7.4에 레이어1 내부 다중 쓰기 레이스 주의(6-7), §7.5 비교표에 인프라·에이전트자동화 호환성 축(6-4) 및 Public/Private 리포 CI 차이(D-3) 추가. 신설: §1.3에 위반 페널티 자동 게이트 연결 원칙(B-3), §1.7 규모별 Tier 1~3 채택 스코프(C-1), §2.4 RBAC+실브라우저 검증 결합 의무(A-2). 체크리스트 항목 8개 갱신/추가 |
| v1.9 | 2026-08-04 | Aiden (Claude, ZEN_CEO) | §7.6 신설 — Edward 요청(자율/반자율 운영 시 에이전트 간 통신 방식, Polling vs Event-Driven). "같은 오케스트레이터 안의 sub-agent"(비동기 완료 알림·재개 메시징·이벤트 스트리밍·파이프라인 의존성 해소 4원시로 이미 해결됨)와 "서로 다른 독립 세션"(ZENITH Team A/B 구조)을 구분. 후자는 GitHub Issue/PR을 공유 블랙보드로 + GitHub Actions 이벤트 트리거로 쓰는 패턴이 이미 실증돼 있으나(§7.1), 개인계정 리포의 `projects_v2_item` 웹훅 미지원으로 순수 event-driven만으로는 부족함이 이미 실측됨을 근거로 "event-driven 우선 + 주기적 재동기화(polling) 백업" 하이브리드를 표준 패턴(Kubernetes controller의 watch+reconcile과 동일 원리)으로 권장. 더 세밀한 핸드오프가 필요하면 Postgres LISTEN/NOTIFY·큐 테이블 옵션을 제시하되, §7.4 "필드별 소유 시스템 하나" 원칙 유지를 위해 GitHub Issue를 유일한 진실로 두고 큐는 배달 수단으로만 역할 제한할 것을 명시. 체크리스트에 항목 1개 추가 |
| v1.10 | 2026-08-04 | Aiden (Claude, ZEN_CEO) | §8 개요 갱신 — 306이 v1.2로 확장되어 §7(R-20 GitNexus 활용성 평가 7-1~7-5)이 추가됨을 반영. 306 §7은 영향도 분석은 DEF-B 사례로 채택하되, CJK/임베딩 미지원·환경 연결(Issue #987)·운영 비용을 근거로 **"영향도 분석 전용" 범위 축소 이식**을 권고(7-5) |
| v1.11 | 2026-08-04 | Aiden (Claude, ZEN_CEO) | Edward 요청(GitNexus 효과성 재검토 + 대안도구 검토 등록, 306/307/308 신규·갱신분 반영). §1.5-a 신설 — 306/308이 서로 다른 모델로 독립 실사해 수렴한 GitNexus 평가(실증 가치: 교차심볼탐색·impact·trace / 제약: staleness·CJK미지원·과잉분석위험·운영비용) + 대안 도구 비교표(TS LSP·madge·ast-grep, 크로스언어 blast radius는 대안 없음) 추가. §1.7에 착수 로드맵 0단계(Tier 확정 우선, 308 §2-1) 보강. §8(신규) 애플리케이션 코드 자산 이식 판정 — 307(Riley/CPO) 4대 자산 제안을 308(D_Kai) 코드실사 재검증 결과로 종합(자산 A 조건부 이식, B·D 미검증 제안으로 재라벨링, C 재설계 항목으로 재정의). 체크리스트 3개 항목 추가, §9로 기존 체크리스트 재배치 |
| v1.12 | 2026-08-04 | Aiden (Claude, ZEN_CEO) | Edward 지시("각 의견이 타당한가 면밀히 검토 후 반영") — v1.11에서 인용한 306/307/308의 핵심 사실관계를 직접 재검증. `.gitnexus/meta.json`(embeddings:0·vectorSearch unavailable·cjkSegmentation none)·인덱스 크기(179MB)·`guards.ts:120`/`status-machine.ts:46`·`ERR_AGENCY_REVERSE_MARGIN` 전체 미존재(0건)·인라인 한글 에러 문자열(195건, "50개 이상" 주장보다 실제로 더 많음)은 실측 확인되어 그대로 유지. **2건 정정**: ①§1.5-a "GitNexus 과잉분석" 사례 — SAR-2026-05-13-001은 "에이전트가 전면분석을 시도"한 게 아니라 "Grep/Glob/Bash 호출마다 Hook이 6,827심볼을 자동 주입"한 구조적 문제였고, **이미 GOV-002/004로 조치 완료**된 역사적 사례임을 명시(현재도 유효한 위험처럼 서술했던 걸 정정) ②§8 자산 C 표 — `OrderStatus` enum 개수를 "14개"에서 실측값 **"13개"**로 정정 + 감사로깅 부재 근거에 파일 경로(`order.repository.ts:203`) 명시 |
| v1.13 | 2026-08-04 | Aiden (Claude, ZEN_CEO) | 307이 v1.1로 개정되어 §4(GitNexus 활용성·한계·4대 대안, Riley/Gemini 작성)가 신설된 것을 확인, 사실관계 검증 후 반영. **검증 통과**: post-commit hook 재인덱싱 방향은 실측 재인덱싱 소요시간(~24.6초, 10,296 노드 기준, 세션 메모리 근거)상 타당 — 단 원안의 `npx gitnexus analyze`는 `rtk` 류 CLI 재작성 훅 환경에서 실패가 실측 확인되어 `gitnexus analyze` 직접 호출로 정정, MCP 리소스가 같은 세션 내 갱신 안 되는 캐싱 문제(세션 재시작 필요)를 보완 명시. MCP 미연결 환경 CLI 폴백+tsc/grep 대체 절차 명문화는 방향만 채택(특정 도구명은 미검증이라 제외). **미검증으로 반영 보류**: "탐색 토큰 60% 이상 절감"(근거 전무, 이전 308이 반증한 "80% 재작업 감축"과 동일 패턴)·"Next.js 동적 호출 탐지 누락"(개연성은 있으나 이 리포에서 확인된 사례 없음)·"심볼 3,000개 이상 Tier 필수화"(305 기존 Tier 게이팅과 중복, 임계값 근거 없음) |
| v1.14 | 2026-08-04 | Aiden (Claude, ZEN_CEO) | §7.7 신설 — Edward 요청(리포지토리 WBS와 ClickUp Task 매핑을 통한 공정관리·공정률 관리 방안, ZENITH_LMS·MYL_MLFF/G_Toll_Platform_clone 양쪽 공통 적용). ZENITH의 체크박스 상태코드 WBS와 G_Toll의 10진법 WBS-ID(`Phase.Group.Task`) 구조를 모두 실측 확인 후 설계. 3계층 분리(WBS 마크다운=구조 기준선/GUIDE, ClickUp=실시간 진행 상태 원본, 가중 롤업=공정률 계산) + 가중 평균 공정률 공식(단순 완료건수 비율 대신 Task별 가중치×완료율) + R-08 원칙 적용(코드 산출물은 자가보고 아닌 PR 머지로 자동 완료 전환) + 단방향 동기화(WBS→ClickUp 1회성 임포트, ClickUp→WBS는 마일스톤 시점 주기적 스냅샷, 실시간 양방향 금지) 제시 |
| v1.15 | 2026-08-04 | Aiden (Claude, ZEN_CEO) | §7.7 보강 — Edward 지적("ZENITH_LMS 관련 내용이 반영됐는가")에 따라 자가 검증 결과, G_Toll 버전(`WBS_ClickUp_Progress_Management_Policy.md`)만큼 ZENITH 구체 사례가 없던 비대칭을 확인하고 보강. `WBS_01_상세_공정표.md` 실측 기반 — 4단계 계층(Phase→L2→L3→L4) ClickUp 매핑 예시, WBS 텍스트에 이미 내재된 `(N MD)` 값을 `가중치` 필드로 그대로 파싱하는 방법, 7종 상태코드(`[x]/[/]/[ ]/[!]/[-]/[>]/[+]`)별 `%완료`·롤업 분모 처리 규칙 표, "잔여 MD" 대시보드 컬럼이 가중 롤업 공식의 부산물임을 명시 |
| v1.16 | 2026-08-04 | Aiden (Claude, ZEN_CEO) | Edward 지시("각 Task는 산출물이 매핑되어야 한다") 반영. **Task-산출물 매핑을 선택이 아닌 필수 요건으로 명문화** — 실측 결과 G_Toll은 이미 산출물 컬럼이 구조화돼 있는 반면 ZENITH `WBS_01_상세_공정표.md`는 "비고" 필드 160건 중 7건(4%)만 파일경로를 언급함을 확인, 과거완료분/향후신규 항목별 대응 방안 제시. 이어 Edward 재질문("이게 실질적 완료검증이 되는가, 더 나은 방법은?")에 답하며 **단일 신호(PR diff 포함 여부)만으로는 vacuous-test류 함정(파일은 건드렸으나 요구사항 미충족)을 못 막는다는 자기 검증 결과를 반영** — R-08(CI 통과)·R-10(UI 증적)·"머지=이미 리뷰+CI 게이트를 통과했다는 프로세스 보증" 3가지를 코드신호와 결합한 4중 신호 체계로 재설계, 4개 신호 모두 충족해야 자동완료 허용·하나라도 빠지면 수동확인으로 남기도록 명시 |
| v1.17 | 2026-08-04 | B_Kai (Big Pickle) | §8 개요 갱신 — 306이 v1.3으로 확장되어 §8(본 문서 v1.11~v1.16 개정 검토 의견 K-1~K-9)이 추가됨을 반영. 306 §8은 §7.7이 체크리스트 미연결(K-1)·자동완료 구현 경로 미정의(K-2)·진실 원천 축 충돌(K-3)의 구조 갭 3건과 `(N MD)` 파싱 엣지케이스(K-4)를 갖는다는 검토 의견을 제시(§1.5-a·§1.7·§8 코드자산 판정은 실증 기반으로 인정) |

---

[← 목록으로 돌아가기](./000_README.md)
